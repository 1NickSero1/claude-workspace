========================================
  AJUSTES — Wallet Control
  Bugs · tareas · mejoras pendientes
  Gestionado por FIXA
========================================

COMANDO RÁPIDO:
  "MEJORAX" = leer este archivo y resolver todo lo pendiente

========================================

------------------------------------------------------------
[PENDIENTE] por MI
------------------------------------------------------------




------------------------------------------------------------
[PENDIENTE] por MAQUINA
------------------------------------------------------------

  — Reactivar "Confirm email" en el dashboard de Supabase
    (Authentication → Providers → Email) antes del lanzamiento público.
    → El código ya está listo para esto (ver RESUELTO más abajo, ronda
      2026-07-06): solo falta el toggle en el dashboard, que hoy sigue
      desactivado a propósito mientras se prueba en local / beta cerrada.

------------------------------------------------------------
[EN PROGRESO]
------------------------------------------------------------
  — Números cortados en la leyenda de los diagramas de torta del
    Resumen (SOLO en la tablet, no reproduce en el celular) —
    reportado 2026-07-27, visto primero en "Metas de ahorro" y
    confirmado también en "Fuentes de ingreso" el mismo día.
    → Durante esta investigación SÍ se encontraron y arreglaron dos bugs
      reales relacionados:
      1) El carrusel de diagramas (donutSlider) había perdido su
         `marginHorizontal: -16` en un arreglo anterior mal aplicado —
         se restauró (ver ronda del 2026-07-26 más abajo). Confirmado
         arreglado en celular Y en tablet para 3 de los 4 diagramas
         (Billetera general, Gastos del mes, Fuentes de ingreso).
      2) El diagrama de "Metas de ahorro" calculaba el % de cada meta
         contra `totalTarget` (la suma de las metas de TODAS las metas)
         en vez de `totalSaved` (lo realmente ahorrado) — con metas
         grandes y poco ahorro esto dibujaba un diagrama casi invisible
         (un hilo delgado en vez de una porción). Corregido en
         resumen.tsx (`total={totalSaved || 1}`).
    → Lo que SIGUE sin explicación: en esa misma tarjeta de "Metas de
      ahorro", en la tablet, los montos de la leyenda ("$5.000",
      "$60.000") se ven cortados (falta el último carácter) de forma
      100% reproducible — probado con reinicio completo de la app,
      limpiando el caché de Expo Go, y con varios arreglos de flexbox
      (minWidth, flexShrink, flexBasis) y de capas nativas de Android
      (collapsable, renderToHardwareTextureAndroid) — ninguno cambió
      nada. El árbol de accesibilidad del dispositivo (que refleja el
      layout real, no lo pintado en pantalla) siempre reporta el monto
      completo y dentro de los límites de la pantalla, igual que en las
      otras 3 tarjetas que SÍ se ven bien — lo que apunta a un problema
      de renderizado específico de esa tablet (posiblemente su GPU/driver
      Android, es una tablet genérica de gama baja), no a un bug de
      layout en el código. Los cambios de flexbox que no ayudaron
      (minWidth:0, flexShrink) se dejaron igual porque son buena práctica
      y no tienen contra, pero no resolvieron el problema visual.
    → Nota aparte: al intentar descartar un caché de bundle viejo en la
      tablet, se corrió `pm clear` sobre Expo Go, lo que borró los datos
      guardados localmente en esa sesión de prueba (cuentas Daviplata/Nu/
      Cajon/Billetera, metas Iphone/Play). El usuario confirmó que era
      data de prueba sin importancia — pero queda como recordatorio de
      que esta app no tiene backend para datos financieros (todo vive en
      AsyncStorage local), así que ese comando nunca debe correrse sobre
      un dispositivo con datos reales sin confirmar antes.
    → Reconectada la tablet el 2026-07-27 (misma sesión) y repetida la
      prueba desde cero (modo anónimo, sin los datos borrados por pm
      clear): se creó una meta "Iphone" ($5.000 de $5.000.000) y luego
      "Play" ($60.000 de $300.000). El corte se reprodujo IDÉNTICO en
      ambas filas ("$5.00" y "$60.00", falta el último carácter en las
      dos) pese a tener largos de texto distintos. El árbol de
      accesibilidad volvió a confirmar que ambos TextView miden el ancho
      correcto para el string completo (53px para "$5.000", 67px para
      "$60.000", terminando ambos en el mismo borde derecho x≈1160 de
      1200) — refuerza que no es un bug de layout/medida de texto.
    → Hallazgo NUEVO: se agregó también un ingreso de prueba ("Test",
      $60.000) y, al deslizar a la pestaña "Fuentes de ingreso" — una de
      las 3 que el reporte anterior daba por buena — el mismo corte
      apareció ahí también ("$60" en vez de "$60.000"). Además, en esa
      misma pantalla aparecieron fragmentos numéricos sueltos ("00") en
      el borde izquierdo, pegados fuera de su tarjeta — residuo de la
      leyenda de la slide anterior (Metas de ahorro) que no se limpió al
      deslizar. Cambiar de pestaña (Tarjetas → Resumen) y volver NO quitó
      ninguno de los dos artefactos — siguen ahí de forma persistente,
      no solo durante la animación del swipe.
      Esto amplía la sospecha original: ya no parece un glitch de
      renderizado de texto exclusivo de "Metas de ahorro", sino algo más
      general de esta tablet con el ScrollView horizontal del carrusel
      (donutSlider) — posible vista no invalidada/reciclada mal en
      Android 10 de gama baja. Sigue sin encontrarse una causa en el
      código: los 4 diagramas comparten exactamente el mismo componente
      de leyenda (`incomeLegendAmt` en resumen.tsx), así que un bug de
      layout tendría que afectar a los 4 por igual y de forma
      determinística, no de forma intermitente por sesión.
    → Prueba definitiva (2026-07-27, misma tarde): a pedido del usuario se
      quitó el `marginLeft: 'auto'` de `incomeLegendAmt` (era el único
      "auto" en todo el layout de la leyenda) y se cambió a un layout
      explícito: `incomeLegendLeft` con `flex: 1` empuja el monto al
      extremo derecho sin depender de auto-margin. Se recargó en vivo en
      la tablet: el corte se reprodujo IDÉNTICO. Como prueba final se le
      dio a `incomeLegendAmt` un `minWidth: 150` (~3 veces más ancho del
      que necesita "$60.000") — el corte siguió exactamente igual, en el
      mismo punto. Esto descarta layout/ancho por completo: con 150px
      garantizados de sobra, si fuera un problema de espacio el texto se
      habría visto completo. El recorte ocurre en el pintado del glyph
      mismo (Skia/GPU de esa tablet), no en la medición/posición de la
      caja — no hay ningún ajuste de CSS/estilo en React Native que pueda
      corregir esto desde el código de la app. El layout quedó simplificado
      de todos modos (sin auto-margin, más idiomático) por limpieza, sin
      el minWidth de prueba.
    → Conclusión final: este bug NO se puede resolver desde el código de
      Wallet Control. Es una limitación de renderizado de esa tablet
      específica (Android 10, gama baja). Se documenta como limitación
      conocida, no como pendiente activo — no seguir invirtiendo tiempo
      de desarrollo aquí salvo que aparezca en más dispositivos reales de
      los amigos que prueben la app (en ese caso sí sería un patrón, no
      un caso aislado de hardware).

------------------------------------------------------------
[RESUELTO]
------------------------------------------------------------

  ✓ Modo anónimo → cuenta real: no existía ningún botón 2026-07-28 (FIXA)
    → La migración de datos (`migrateNamespaceData`) ya existía y funcionaba
      desde la ronda de auditoría, pero solo se disparaba si el usuario
      pasaba por el wizard completo de "Crear cuenta" — y a ese wizard solo
      se llegaba cerrando sesión primero, lo cual (correctamente, por
      diseño) BORRA los datos del modo anónimo antes de que alcanzaran a
      migrar. En la práctica no había ninguna forma de aprovechar la
      migración: entrar en modo anónimo era un callejón sin salida para los
      datos.
    → Se agregó un botón "Crear cuenta y guardar mis datos" en Perfil,
      visible solo en modo anónimo, que abre el registro directamente
      (`/onboarding?step=register`) sin cerrar sesión ni borrar nada.
    → Al registrarse desde ahí, se detecta que venía de modo anónimo y: se
      migran tarjetas/categorías/metas/gastos/ingresos/presupuesto al
      namespace de la cuenta nueva, se conservan sus preferencias
      (periodicidad, umbral de gasto hormiga) y se salta el wizard de
      "¿tienes ingreso fijo?/¿cuánto gastas en fijos?" (ya tiene esos datos
      reales, repetirlo los habría duplicado) — va directo a la app.
    → De regalo: el texto de "Cerrar sesión" ahora avisa explícitamente en
      modo anónimo que se borrarán los datos si no creas cuenta antes
      (antes decía, incorrecto, que los datos "se mantendrían en el
      dispositivo" incluso en modo anónimo). Y `handleLogin` también
      intenta la migración como red de seguridad, por si el registro quedó
      pendiente de confirmar correo mientras tanto.
    → npx tsc --noEmit: 0 errores.

  ✓ Tope de 12 dígitos, faltaba en 12 campos más de monto 2026-07-28 (FIXA)
    → Confirmado en pruebas: en tarjetas.tsx (agregar dinero, editar abono,
      editar gasto) y CardFormModal.tsx (límite/saldo) el tope sí
      funcionaba, pero el mismo campo de "editar gasto" en resumen.tsx
      (desde "Gastos del mes", una pantalla distinta a la de tarjetas.tsx)
      nunca lo tuvo — de ahí el reporte de "permite un valor exagerado".
      Al revisar se encontró que el tope solo se había aplicado en la
      ronda anterior a los 5 campos de Tarjetas, y quedaban 12 más sin
      protección en el resto de la app: CategoryDetailModal, BudgetFormModal
      (presupuesto, usado en Personalización Y onboarding), onboarding.tsx
      (presupuesto, ingreso fijo, gasto fijo, umbral de gasto hormiga),
      gastos-recurrentes.tsx (nuevo recurrente), resumen.tsx (editar gasto,
      editar ingreso, aportar a meta, crear meta) y QuickEntryModal.tsx
      (Finando IA). Se agregó `.slice(0, 12)` a los 12 — ya no queda
      ningún campo de monto sin tope en toda la app.
    → npx tsc --noEmit: 0 errores.

  ✓ Últimos Alert.alert nativos reemplazados 2026-07-27 (FIXA)
    → Quedaban 12 llamadas reales a Alert.alert (el popup genérico blanco/
      negro de Android) sin reemplazar por el tema de la app, repartidas en
      4 archivos: CardFormModal.tsx (notificaciones desactivadas, límite
      por debajo de lo gastado), onboarding.tsx (confirma tu correo,
      errores de registro/login), ayuda.tsx y resumen.tsx (PDF
      generado/error al exportar reporte y extracto).
    → Se creó `components/InfoModal.tsx`, un modal genérico reutilizable
      (título + mensaje + botón OK) con el mismo estilo ya usado en los
      demás reemplazos de Alert.alert de la app (CategoryDetailModal,
      categorias.tsx, tarjetas.tsx, perfil.tsx) — 4 variantes de color
      (info/success/warning/error) según el tipo de mensaje.
    → Con esto ya NO queda ningún Alert.alert real en la app — todos los
      popups que ve el usuario son del tema de Wallet Control.
    → npx tsc --noEmit: 0 errores.

  ✓ Ronda de pruebas post-auditoría 2026-07-27 (FIXA)
    → Ingresos editables/eliminables deslizando: confirmado que SÍ
      funciona — el código ya estaba correcto (idéntico al de gastos);
      lo que fallaba era un servidor de Expo viejo, sin caché limpia,
      que seguía corriendo de una sesión anterior. Se cerró y se dejó
      uno solo, limpio, en el primer puerto libre.
    → Selector de tarjetas en "Pagar" (CardView.tsx, modo `compact`):
      en tablet (pantalla ancha) el escalado automático (moderateScale)
      casi duplicaba el padding y la letra, por eso seguía viéndose
      grande aunque ya se había achicado antes. Se sacó ese selector del
      escalado automático y se le dejó tamaño fijo, igual en celular y
      tablet. Usuario confirmó que el tamaño quedó perfecto — queda
      BLOQUEADO, no cambiar sin permiso explícito (ver comentario en el
      código).
    → Personalización: el mensaje "Ajusta la app a tu manera" solo
      mencionaba el gasto hormiga — se generalizó para mencionar también
      periodicidad, presupuesto e ingreso fijo.
    → npx tsc --noEmit: 0 errores.

  ✓ Auditoría 2026-07-27 — Última fase del plan: mutex de storage.ts
    (PECAS)
    → "Condición de carrera en cada guardado": dos guardados casi
      simultáneos sobre el mismo dato (ej. dos gastos agregados muy
      seguidos, o abrir la app en tablet y celular a la vez sobre la
      misma cuenta) podían leer el mismo estado viejo y el segundo en
      terminar pisaba lo que guardó el primero. Ahora cada función que
      guarda/borra/edita algo (tarjetas, categorías, metas, gastos,
      ingresos, presupuesto, recurrentes) encola su lectura+escritura
      detrás de cualquier otra que esté tocando el MISMO dato en ese
      momento — invisible para el usuario, no cambia ninguna pantalla.
    → Con esto queda completo el plan de los 39 hallazgos de la auditoría
      del 2026-07-27 (fases 0 a 7 + este mutex final), más los pendientes
      "por MI" que se sumaron en el camino (ingreso fijo/Sueldo,
      categorías predeterminadas, tamaño de tarjetas, conteo de
      recurrentes). npx tsc --noEmit: 0 errores en cada paso.
    → Pendiente: probar todo en el dispositivo (todas las fases juntas,
      según lo acordado) y correr `eas env:list` antes del próximo
      "SUBIR PROYECTO" (ver Fase 7 más abajo).

  ✓ Auditoría 2026-07-27 — Fase 7 del plan: arreglos independientes
    (PECAS)
    → "Variables de Supabase sin guardia": si faltan las env vars de
      Supabase, ahora la app lanza un error claro en vez de fallar de
      forma confusa más adelante. Pendiente operativo (no es código):
      correr `eas env:list` y confirmar que estén registradas para el
      perfil "preview" antes del próximo "SUBIR PROYECTO".
    → "formatCOP con signo mal puesto": un monto negativo se mostraba
      "$-5.000" en vez de "-$5.000".
    → "formatThousands vacía el campo en 0": escribir "0" en un monto
      borraba el campo en vez de mostrar "0".
    → "Finando IA se cuelga sin red": el chat con IA real ahora tiene un
      límite de 20 segundos — si no hay respuesta, muestra un error en vez
      de quedarse pensando indefinidamente.
    → "Finando ignora la tarjeta nombrada": si le dices a Finando con qué
      tarjeta pagaste (ej. "con Nequi"), el gasto ahora queda asignado a
      esa cuenta de una vez, en vez de quedar sin tarjeta asignada.
    → "Regex de modo demo trunca montos": en modo demo (sin cuenta real),
      montos con más de un punto de miles (ej. "$1.800.000") se
      registraban truncados (ej. "$800.000"). Ya captura el monto completo.
    → "Cerrar sesión no cancela notificación de saldo": ya se cancela al
      cerrar sesión, en ambos modos (cuenta real y anónimo).
    → "Doble navegación al cerrar sesión": en cuenta real, ya no compite
      la navegación manual con la que dispara sola la app al detectar el
      cierre de sesión (se dejó solo en el modo anónimo, que sí la necesita).
    → "Ayuda desactualizada + contradicción de API key": la guía en PDF ya
      describe los 4 diagramas reales de Resumen (antes hablaba de "3
      segmentos" y una "barra de ahorro" que ya no existen) y los 3 chips
      de acceso rápido reales; se borró la línea que sugería que hacía
      falta configurar una API key (nunca hace falta).
    → npx tsc --noEmit: 0 errores.

  ✓ Auditoría 2026-07-27 — Fase 6 del plan: completitud de datos —
    ingresos editables, presupuesto con respaldo del mes anterior (PECAS)
    → "Ingresos no editables/eliminables": el popup "Fuentes de ingreso"
      en Resumen ahora permite deslizar cada ingreso para editarlo o
      eliminarlo, igual que ya se podía con los gastos.
    → "Presupuesto no se traslada de mes a mes": si un mes nuevo no tiene
      presupuesto configurado todavía, ahora se muestra (solo para
      referencia, sin guardarlo) el del mes anterior, tanto en
      Personalización como en Resumen — para no ver "Sin configurar" cada
      mes si ya lo habías definido antes. El aviso de 80%/100% del gasto
      sigue mirando solo el presupuesto explícito de ese mes exacto, para
      no dispararse sobre un valor que el usuario nunca confirmó para el
      período actual.
    → npx tsc --noEmit: 0 errores.

  ✓ Auditoría 2026-07-27 — Fase 5 del plan: CategoryDetailModal +
    resumen.tsx, sincronía nombre/fecha en recurrentes (PECAS)
    → "Renombrar recurrente deja notificación con nombre/fecha viejos": al
      editar un gasto recurrente (tanto desde una categoría como desde el
      popup "Gastos del mes" de Resumen), cambiar el nombre, la frecuencia
      O la fecha de vencimiento ahora reprograma el recordatorio — antes
      solo reprogramaba si cambiaba la frecuencia.
    → "Recordatorio ignora la fecha elegida": el recordatorio ahora se
      agenda para la fecha real que el usuario eligió (no siempre "hoy").
      De paso se corrigió que, al editar un gasto que YA tenía una fecha
      guardada, esa fecha no se cargaba en el selector si no se tocaba el
      calendario — quedaba en blanco por dentro aunque se viera el texto.
    → "Sin ajuste para meses cortos": un recordatorio agendado para el día
      29, 30 o 31 simplemente no sonaba en meses que no llegan a esa
      fecha (ej. febrero) — ahora se limita a máximo el día 28.
    → npx tsc --noEmit: 0 errores.

  ✓ Auditoría 2026-07-27 — Fase 4 del plan: Recurrentes/Categorías, dedup
    y notificaciones (PECAS)
    → "Doble conteo de recurrentes": splitRecurringByPaid comparaba TODOS
      los gastos fijos contra la periodicidad global del dashboard, en vez
      de la frecuencia propia de cada uno (semanal/quincenal/mensual) —
      ahora compara cada plantilla contra su propia ventana, evitando
      conteos duplicados o inconsistentes entre pendientes/pagados.
    → "Pagar pierde la fecha de vencimiento": al marcar un gasto fijo como
      pagado, el gasto real creado ya no pierde la fecha de vencimiento
      configurada en la plantilla (recurrenceDueDate).
    → "Crear recurrente con nombre repetido": el hub "Gastos recurrentes"
      ahora avisa (en vez de crear un duplicado silencioso) si ya existe
      un gasto fijo con el mismo nombre en la misma categoría.
    → "Colisión de key en React": dos categorías eliminadas distintas
      colisionaban en la misma key literal "sin-categoria" al agrupar
      pendientes/pagados — ahora se usa el categoryId real como key.
    → "Crear recurrente desde el hub no agenda notificación": ahora agenda
      recordatorio igual que el resto de la app, y "Reversar" (borrar la
      definición antes de pagarla) cancela esa notificación primero.
    → "Sin validación de nombre duplicado en categorías": el formulario de
      categoría ya no deja guardar un nombre repetido (case-insensitive),
      mismo patrón de aviso + botón deshabilitado que el resto de la app.
    → npx tsc --noEmit: 0 errores.

  ✓ MEJORAX — quitar categorías predeterminadas + acotar el conteo de
    gastos fijos a lo creado en Gastos recurrentes (2026-07-27)
    → "Quita las categorías predeterminadas y deja las que crea el
      usuario" (confirmado: borrarlas también de la cuenta actual): ya no
      se siembran las 10 categorías por defecto (Vivienda, Comida, etc.)
      para ninguna cuenta nueva — `getCategories()` devuelve vacío si el
      usuario no ha creado ninguna. Además, una limpieza automática nueva
      (una sola vez por cuenta, corre sola la próxima vez que la app cargue
      datos) quita las que ya estaban guardadas de antes en CUALQUIER
      cuenta de este dispositivo (la tuya y la de tu hermano si usa la
      misma app) — los gastos que ya tenían una de esas categorías quedan
      sin categoría asignada (mismo efecto que borrar cualquier categoría
      a mano, tal como confirmaste). El paso de "gastos fijos" del
      onboarding (que antes asumía que la categoría "Otro" siempre
      existía) ahora crea una categoría real ("Otros") solo si hace falta,
      para que esos gastos no queden invisibles en "Gastos por categoría".
      También se agregó un aviso en "Registrar gasto" si todavía no hay
      ninguna categoría creada, con la pista de dónde crearla.
    → "Vincula las categorías que el usuario crea en las pills de gastos":
      las píldoras de categoría (Registrar gasto, Finando IA) ya leían de
      la misma lista de categorías del usuario — con los defaults fuera,
      esas píldoras ahora muestran únicamente lo que el usuario creó, sin
      cambios de código adicionales más allá de lo anterior.
    → "Suma solo los gastos recurrentes creados en la pantalla de
      recurrentes, no sumes algo más": el contador "X de Y gastos fijos
      pagados" de cada Quincena en Resumen contaba también los gastos
      marcados "recurrente" desde otro lado (ej. al editar uno dentro de
      una categoría), no solo los creados en Gastos recurrentes. Ahora
      getRecurringTemplates() conserva el id de la definición original
      incluso después de pagarla por primera vez, y el contador de
      Quincena filtra solo por esas. El popup "Pendientes por pagar" no se
      tocó — sigue mostrando todos, como se confirmó en una ronda anterior.
    → npx tsc --noEmit: 0 errores.

  ✓ MEJORAX — ingreso fijo mensual editable/nuevo desde Personalización +
    renombrado a "Sueldo" + tarjetas más pequeñas en selectores de pago
    (2026-07-27)
    → "Editar o poner un ingreso fijo mensual nuevo desde Personaliza tu
      vida financiera": nueva sección en personalizacion.tsx, mismo patrón
      que Presupuesto/Gasto hormiga. Si ya existe uno este mes, corrige
      solo el monto (nueva `updateIncome` en storage.ts); si no existe
      todavía, sigue el mismo flujo que el onboarding (registra el
      ingreso, agenda recordatorio, fondea/crea la cuenta de Efectivo).
    → "Ponle nombre sueldo": el ingreso fijo mensual (tanto el creado en
      onboarding como el creado/editado desde Personalización) ahora se
      guarda como "Sueldo" en vez de "Ingreso fijo mensual" — así aparece
      en "Fuentes de ingreso" y en el recordatorio.
    → "Las tarjetas se ven muy grandes en la opción de pagar": el modo
      compact de CardView (usado en Registrar gasto, Pagar recurrente y
      editar gasto) se hizo más angosto y compacto (minWidth 148→122,
      menos padding, textos más pequeños) en los 3 lugares que lo usan.
    → npx tsc --noEmit: 0 errores.

  ✓ Auditoría 2026-07-27 — Fase 3 del plan: Tarjetas, resto de arreglos
    locales (PECAS)
    → "Etiqueta 'Débito' en préstamo sin emoji": la insignia de la hoja de
      acciones de un préstamo sin emoji propio mostraba "Débito" — ahora
      muestra 💸, igual que la vista previa de CardFormModal.
    → "Editar abono no valida contra la deuda": handleSaveEvent (editar un
      abono ya registrado) ahora reutiliza el mismo modal "Abono mayor a
      la deuda" que ya usaba abonar por primera vez, devolviéndole el
      monto viejo al pendiente antes de comparar.
    → "Editar gasto no valida contra saldo/cupo disponible": el modal de
      editar gasto en Tarjetas ahora usa getCardAvailable (mismo helper
      que Resumen) con aviso en rojo + "Guardar" deshabilitado si el monto
      supera lo disponible en la cuenta asignada.
    → "Sin tope en montos": los 3 campos de monto de tarjetas.tsx que no
      pasaban por el sanitizador de CardFormModal ahora cortan a 12
      dígitos.
    → npx tsc --noEmit: 0 errores. Pendiente de prueba en dispositivo.

  ✓ Auditoría 2026-07-27 — Fase 2 del plan: Tarjetas, eliminar tarjeta +
    validación de cupo (PECAS)
    → "Bajar el límite de crédito no avisa si ya hay más gastado que el
      nuevo límite": CardFormModal ahora recibe `expenses` (pasado desde
      tarjetas.tsx) y, al guardar una tarjeta de crédito existente, compara
      el nuevo límite contra getCardTotalSpent — si queda por debajo,
      muestra un aviso (no bloquea el guardado, es solo informativo).
    → "Eliminar tarjeta no cancela su notificación de vencimiento": ya
      arreglado en tarjetas.tsx (confirmDeleteCardNow ahora llama a
      cancelNotification antes de borrar la tarjeta) — mismo bug que ya
      se había corregido para "eliminar gasto" en resumen.tsx.
    → "Eliminar tarjeta deja gastos huérfanos sin avisar": según la
      decisión ya confirmada con el usuario (los gastos se conservan en el
      historial, solo quedan sin cuenta asignada), confirmDeleteCardNow
      ahora llama a clearCardFromExpenses antes de borrar la tarjeta, y el
      modal de confirmación de borrado muestra cuántos gastos quedarán así
      antes de que el usuario confirme.
    → npx tsc --noEmit: 0 errores. Pendiente de prueba en dispositivo junto
      con la Fase 1 (a pedido del usuario, se agrupan para probar las dos
      juntas en la siguiente ronda).

  ✓ MEJORAX — selector de tarjeta más visual + acceso a categorías desde
    Personalización (2026-07-27)
    → "Elegir la cuenta del gasto más grande/con previsualización": los 3
      lugares donde se elige de qué cuenta sale el dinero (Registrar gasto,
      editar gasto desde "Gastos del mes" en Resumen, y marcar un
      recurrente como pagado) usaban una simple píldora de texto gris.
      Se reemplazaron por el componente CardView en su modo `compact` (ya
      existía y se usaba en el selector de tarjeta de Finando IA) — ahora
      cada opción se ve con el color real de la tarjeta, su emoji, y el
      saldo/disponible actual. Se aprovechó para agregar el monto
      disponible también al modo compact (antes solo lo mostraba la
      tarjeta completa), incluyendo cuentas de Efectivo (antes esa
      cuenta no calculaba saldo disponible en CardView en absoluto).
    → "Crear/modificar categorías desde Personaliza tu cuenta": se
      encontró que app/categorias.tsx ya no tenía NINGÚN punto de entrada
      en toda la app (quedó huérfana desde que la tarjeta de Resumen que
      antes llevaba ahí se redirigió a Gastos Recurrentes, ronda
      2026-07-22). Se agregó una nueva sección "Categorías de gastos" en
      Personaliza tu vida financiera que navega a /categorias — de paso
      resuelve que la pantalla fuera inalcanzable.
    → npx tsc --noEmit: 0 errores.

  ✓ Cuenta nueva con ingreso fijo no dejaba registrar ningún gasto
    (reportado 2026-07-27, durante las pruebas de los arreglos de la
    auditoría del mismo día)
    → Causa: "Ingresos" y "Cuentas" (tarjetas/efectivo/débito) son dos
      cosas totalmente separadas en el modelo de datos. El paso de
      onboarding "¿Tienes un ingreso fijo mensual?" solo guardaba un
      registro de Income — nunca fondeaba ninguna cuenta real. Como
      "Registrar gasto" (QuickEntryModal) solo deja elegir cuentas con
      saldo disponible (`getPayableCards`), un usuario nuevo sin tarjetas
      creadas quedaba sin ninguna opción para pagar, con el aviso
      "Ninguna cuenta tiene saldo disponible ahora mismo" bloqueando por
      completo el primer gasto.
    → Fix: en app/onboarding.tsx (`handleFixedIncomeContinue`), además de
      guardar el Income, ahora se fondea (o se crea, si no existe) una
      cuenta de Efectivo con ese mismo monto — así el ingreso fijo mensual
      sí llega a un lugar gastable desde el primer momento, como se
      esperaba.
    → npx tsc --noEmit: 0 errores.

  ✓ Línea/barra negra fija encima de la tab bar + borde transparente en
    popups que deja ver la tab bar detrás (SOLO en el celular, reportado
    2026-07-27)
    → Barra negra fija: en app/(tabs)/_layout.tsx, tabBarStyle combinaba
      `elevation: 8` (sombra de Android) con `shadowOpacity: 1` (opacidad
      total) y shadowColor semi-transparente — en Android, shadowOpacity:1
      puede pintar la sombra de elevation casi sólida en vez de sutil,
      dependiendo del renderer/skin del fabricante. Se separó el estilo
      por plataforma: Android ahora solo usa `elevation: 4` (sin shadow*,
      esas props son de iOS), iOS conserva la sombra con shadowOpacity
      bajado a 0.15.
    → Borde transparente en popups (se ve la tab bar detrás): los ~26
      `<Modal transparent>` del proyecto (BottomSheet.tsx y los sheets
      sueltos de resumen.tsx, tarjetas.tsx, categorias.tsx, perfil.tsx,
      CardFormModal.tsx, CategoryDetailModal.tsx, PayRecurringModal.tsx)
      no llevaban `statusBarTranslucent`. En Android, sin ese flag, la
      ventana nativa del Modal no siempre se extiende hasta el borde real
      de la pantalla (por debajo de la barra de gestos) en ciertos
      teléfonos — se ve literalmente la pantalla de atrás (la tab bar) por
      esa rendija. Agregado `statusBarTranslucent` a los 26 Modal (fix
      estándar y bien documentado en RN/Android para este síntoma exacto).
    → No se pudo probar en vivo en el celular real en esta sesión (solo la
      tablet estaba conectada por ADB) — pendiente que el usuario confirme
      en su teléfono tras recargar la app (forzar cierre de Expo Go +
      reabrir si el reload normal no toma el bundle nuevo, lección ya
      conocida de la ronda del 2026-07-26).
    → npx tsc --noEmit: 0 errores.

  ✓ MEJORAX — onboarding personalizado + diagramas vacíos abren el "+" (2026-07-27)
    → "Preguntas personalizadas al crear cuenta nueva": el onboarding ya traía
      pasos de periodicidad, ingreso fijo y gastos fijos (para cuenta real Y
      modo anónimo), pero faltaba el presupuesto mensual — hoy solo se
      configuraba después, a mano, desde Perfil → Personaliza tu vida
      financiera. Se agregó un paso nuevo "¿Cuál es tu presupuesto mensual?"
      en onboarding.tsx, entre periodicidad e ingreso fijo, opcional/omitible
      igual que los demás, que llama a saveBudget(monthKey, monto). Probado
      en la tablet real (modo anónimo): al terminar el onboarding, la tarjeta
      "Presupuesto mensual" en Resumen ya aparece configurada ($1.500.000)
      en vez de pedir configurarlo en Perfil.
    → "Diagrama de torta vacío → abrir el '+' al tocar el círculo": en
      resumen.tsx, los donuts de Gastos del mes, Metas de ahorro y Fuentes
      de ingreso ignoraban el toque por completo cuando no había datos (el
      texto ya sugería "Toca + para..." pero tocar el círculo no hacía
      nada). Ahora, si no hay datos, tocar el donut abre la cápsula "+"
      (Ingreso/Gasto/Meta) — igual que tocar el botón flotante. Con datos,
      sigue abriendo el popup de detalle de siempre. Billetera general
      (cuentas/tarjetas) se dejó igual a propósito: sus datos se crean desde
      la pestaña Tarjetas, no desde la cápsula de registro, así que abrir el
      "+" ahí no resolvería nada.
    → Probado en la tablet real: el donut de "Gastos del mes" vacío ahora
      abre la cápsula al tocarlo. La misma lógica (idéntica en los 3 casos)
      se dejó para Metas e Ingresos.
    → npx tsc --noEmit: 0 errores.

  ✓ MEJORAX — décima ronda de pendientes "por MI" del 2026-07-26 (FIXA/PECAS)
    → "Mismo swipe de editar/eliminar en Efectivo": en tarjetas.tsx, las
      filas de la sección Efectivo quedaron envueltas en SwipeableRow
      (onEdit/onDelete), igual que ya se había hecho con Préstamos en la
      ronda anterior.
    → "Swipe de editar/eliminar en cada transacción del popup de Gastos del
      mes": el gesto ya estaba implementado en el código (SwipeableRow por
      fila), pero no funcionaba en Android — causa real: ese popup es un
      <Modal>, que en Android se monta en su propia ventana nativa aparte,
      y el GestureHandlerRootView que envuelve toda la app (en _layout.tsx)
      no llega a cubrirla. Se agregó un GestureHandlerRootView propio
      envolviendo el contenido del modal en resumen.tsx — con eso el swipe
      sí responde dentro del popup.
    → "Arreglar el destello en toda la app": se agregó android.backgroundColor
      ("#0A0E1A", el mismo fondo oscuro de la app) en app.json, que fija el
      color de fondo nativo de la Activity de Android (distinto del
      backgroundColor del plugin expo-splash-screen, que solo aplica al
      splash inicial). Ojo: esto requiere un build nativo (EAS build) para
      tomar efecto — en Expo Go no se nota ningún cambio, es esperado.
    → Bug real encontrado en vivo (no estaba en ningún pendiente): en la
      tablet, al deslizar entre los diagramas de Resumen (Billetera general
      / Gastos del mes / Metas de ahorro / Fuentes de ingreso), quedaban
      pegados fragmentos de números de la leyenda del diagrama anterior,
      visibles incluso sin tocar la pantalla. Causa real: el carrusel
      (ScrollView con pagingEnabled) mide su ancho de página según su propio
      recuadro renderizado, que dependía del contenedor donde vive cada uno:
        · Los diagramas (donutSlider) viven dentro de heroCard, que tiene
          margen Y padding horizontal (32px en total) — su recuadro real
          quedaba 32px más angosto que el ancho declarado en cada slide
          (cardWidth = ancho de pantalla - 32), así que cada diagrama se
          desbordaba sobre el siguiente.
        · Los carruseles de Quincena y Balance General (quincenaSlider/
          balanceSlider) viven dentro de budgetWrap, que solo tiene margen
          (sin padding aparte) — ahí el recuadro real ya coincidía con
          cardWidth sin ajuste extra.
      Primer intento de arreglo (quitar el margen negativo de los tres por
      igual, asumiendo que compartían la misma estructura) fue incorrecto y
      alcanzó a subirse sin detectarlo — quedó "arreglado" en Quincena/
      Balance pero SIGUIÓ desbordándose en los diagramas (fue lo que el
      usuario detectó de nuevo en su celular físico, pensando que era un
      cambio de "centrado"). Arreglo real: devolver el margen negativo
      (marginHorizontal: -16) SOLO al carrusel de diagramas (donutSlider),
      que es el único cuyo contenedor padre necesita esa compensación.
      Verificado deslizando las 4 páginas en la tablet Y en el celular
      físico (Samsung A24): ningún fragmento pegado, números completos.
    → Verificado con `npx tsc --noEmit` sin errores y en vivo en ambos
      dispositivos conectados (tablet por USB, celular por wifi).

  ✓ MEJORAX — novena ronda de pendientes "por MI" del 2026-07-26 (FIXA/PECAS)
    → "Pantalla de personalización de vida financiera": nueva pantalla
      app/personalizacion.tsx ("Personaliza tu vida financiera"), navegable
      desde Perfil (tarjeta "Tu experiencia"). Se sacaron de Perfil y se
      movieron ahí las 3 secciones que trataban de la cuenta financiera en
      sí (Periodicidad de cuenta, Presupuesto mensual, Gasto hormiga) — en
      Perfil quedó solo lo de identidad/app (nombre, tema, idioma, versión,
      notificación de saldo, cerrar sesión).
    → "Icono de volver/deshacer por una X, y donde está la X de pagados
      pon el de deshacer": en gastos-recurrentes.tsx se intercambiaron los
      íconos entre "Reversar" (Pendientes, antes arrow-undo-outline) y
      "Marcar no pagado" (Pagados, antes close) — mismas acciones, íconos
      cruzados.
    → "Pop up al pagar si la tarjeta no tiene esa plata, para recargar o
      cambiar método": PayRecurringModal ya no recibe solo las cuentas con
      saldo suficiente (getPayableCards) — ahora recibe TODAS las cuentas
      gastables sin filtrar por saldo (nueva getSpendableCards en
      recurringPayments.ts) y valida al tocar "Confirmar": si el disponible
      de la cuenta elegida es menor al monto del gasto fijo, muestra un
      paso nuevo "Fondos insuficientes" con dos botones — "Recargar cuenta"
      (cierra el modal y navega a Tarjetas) y "Cambiar método de pago"
      (vuelve al selector de cuentas sin perder el gasto fijo elegido).
      Aplicado en los 3 lugares que abren este modal (resumen.tsx,
      categorias.tsx, gastos-recurrentes.tsx).
    → "Montos del diagrama de Gastos del mes en rojo y negativo": la
      leyenda por categoría de ese donut (resumen.tsx) usaba el color verde
      por defecto de incomeLegendAmt — ahora fuerza COLORS.danger y un "-"
      antes del monto, igual que ya hacían las filas de deuda del mismo
      diagrama.
    → "Calendario opcional al registrar un ingreso": QuickEntryModal ahora
      muestra un ícono de calendario junto al campo Descripción (solo en la
      pestaña Ingreso) que abre el DateTimePicker nativo; si se elige una
      fecha, el ingreso se guarda con esa fecha (createdAt) y su quincena
      correspondiente en vez de la fecha de hoy.
    → "Desplazable en Gastos del mes para editar/eliminar": ya estaba
      implementado (SwipeableRow con onEdit/onDelete en cada fila del popup
      de resumen.tsx) — no requirió cambios, solo se confirmó en código y
      en vivo.
    → "Quitar el + de Metas de ahorro": se quitó el botón "+" pequeño del
      encabezado de la sección "Metas de ahorro" en Resumen — crear una
      meta nueva sigue disponible desde la cápsula del botón "+" morado
      principal (🎯 Nueva meta de ahorro), que ya cubría lo mismo.
    → "Quitar el pop up de préstamo, usar el desplazable de editar/
      eliminar": en tarjetas.tsx, cada fila de Préstamos ahora está envuelta
      en SwipeableRow (onEdit abre CardFormModal, onDelete abre la
      confirmación de eliminar). El popup que se abre al tocar la fila
      sigue existiendo (para ver deuda inicial/pendiente/fecha límite y
      abonar), pero ya NO muestra los botones "Editar"/"Eliminar" cuando la
      tarjeta es un préstamo — esos dos quedaron solo en el swipe. Los
      demás tipos de cuenta (débito/crédito/efectivo) no cambiaron: siguen
      con Editar/Eliminar dentro del popup, igual que antes.
    → "Bloquear que una tarjeta quede en negativo": nueva getCardAvailable
      en recurringPayments.ts (disponible real de una cuenta = cupo o saldo
      menos lo gastado). Se usa para validar el monto contra la cuenta
      elegida en QuickEntryModal (registro rápido de gasto) y en el modal
      "Editar gasto" de resumen.tsx (que antes dejaba elegir cualquier
      tarjeta sin límite) — si el monto supera lo disponible, "Guardar"
      queda deshabilitado con un aviso en rojo. Al editar, el disponible le
      devuelve primero el monto viejo de ese mismo gasto antes de comparar,
      para no bloquear guardar un gasto sin cambios reales de monto.
    → "Editar manteniendo presionado en Historial reciente": las filas de
      gasto ahí ya se podían editar (con un tap, desde antes). Lo que
      faltaba de verdad eran los abonos/depósitos (CardEvent), que no tenían
      ninguna forma de editarse o borrarse. Nuevas updateCardEvent/
      deleteCardEvent en storage.ts + long press sobre cualquier abono o
      depósito del historial reciente (tarjetas.tsx) abre un formulario para
      cambiar el monto/nota o eliminarlo — ajustando el saldo/deuda de la
      tarjeta según corresponda (revierte el efecto del monto viejo antes
      de aplicar el nuevo).
    → Verificado con `npx tsc --noEmit` sin errores en cada paso y en vivo
      en la tablet: pantalla de Personalización con sus 3 valores reales,
      íconos intercambiados en Gastos recurrentes, montos en rojo/negativo
      en el diagrama, "Metas de ahorro" sin el + redundante, calendario del
      ingreso mostrando el día elegido junto al ícono, el popup de préstamo
      sin Editar/Eliminar (solo Abonar), y el flujo completo de "Fondos
      insuficientes" (Daviplata $50.000 vs ARRIENDO $450.000) mostrando el
      aviso y volviendo al selector de cuentas con "Cambiar método de pago".
      El swipe en sí (gesto) no se pudo confirmar visualmente por la misma
      limitación de automatización ya documentada en rondas anteriores —
      se confirmó por código y por el popup de préstamo ya sin los botones.

  ✓ MEJORAX — octava ronda de pendientes "por MI" del 2026-07-22 (FIXA)
    → El pedido original ("que no diga presupuesto... ver resumen, y que se
      diferencien los gastos por categoría, recurrentes/no recurrentes, e
      ingresos") era ambiguo — se aclaró con el usuario a qué tarjeta se
      refería y qué tan grande era el cambio antes de tocar código.
    → Tarjeta "Presupuesto mensual" (debajo del carrusel de diagramas en
      Resumen): se agregó la leyenda "Toca para ver el resumen del mes →"
      al final de la tarjeta, para que quede claro que tocarla abre el
      popup "Resumen del mes" (ya existía esa navegación, solo faltaba la
      pista visual — mismo patrón que "Toca para ver detalle →" ya usado en
      Balance General).
    → A pedido del usuario en la misma ronda, se quitó la línea
      "Presupuesto: $X" que estaba al final de esa misma tarjeta (quedaba
      redundante junto a "Disponible", que ya muestra cuánto queda del
      presupuesto).
    → Popup "Resumen del mes": se agregó una sección nueva "🔁 Recurrentes
      vs no recurrentes", entre "Gastos por categoría" y "Fuentes de
      ingreso" (mismo orden que pidió el usuario) — muestra el total y % de
      lo gastado en gastos marcados como recurrentes vs los que no, mismo
      estilo visual (barra + %) que ya usaba "Gastos por categoría". Las
      otras dos secciones (categoría, ingresos) ya existían — no hubo que
      duplicar esa lógica.
    → Verificado con `npx tsc --noEmit` sin errores y en vivo en la tablet:
      la leyenda nueva se ve en la tarjeta, la línea de presupuesto ya no
      aparece, y el popup muestra "Recurrentes $45.000 (35%) / No
      recurrentes $83.000 (65%)" correctamente calculado sobre los gastos
      reales del mes.

  ✓ MEJORAX — séptima ronda de pendientes "por MI" del 2026-07-22 (FIXA)
    → "No pongas 650k sino $650.000": el centro del donut "Billetera general"
      usaba fmtShort() (abreviaba a "$650k"/"$1.2M"). Se cambió a formatCOP()
      para mostrar el monto completo, igual que ya hacían los otros 3 donuts
      del carrusel. fmtShort() quedó sin ningún uso — se eliminó.
    → "En Quincena 1 y 2, ponle al final un total de lo que se gasta en cada
      una": se re-agregó (se había quitado en la ronda anterior, ver v1.26)
      un texto con el total gastado en esa quincena específica, esta vez AL
      FINAL de la tarjeta (después de la barra de progreso y del contador
      "X de Y gastos fijos pagados"), no antes como en su primera versión —
      nueva función quincenaSpent(q).
    → "Que el diagrama de gastado solo sume lo que se va gastando, sin
      contar los gastos recurrentes hasta la fecha que suelen pagarse":
      el donut "Gastos del mes" (y su leyenda + valor central) ahora excluye
      un gasto recurrente con fecha de pago configurada (recurrenceDueDate)
      SI esa fecha todavía no ha llegado dentro del mes — aunque el gasto ya
      esté registrado, no se suma al total "gastado" hasta el día que
      corresponde. Si el gasto recurrente NO tiene fecha de pago configurada
      (es un campo opcional), sigue contando de inmediato al registrarse,
      igual que antes — no había una fecha de referencia para excluirlo.
      Este cambio quedó acotado SOLO a ese diagrama: la tarjeta "Gastos fijos
      mensuales", el popup de detalle "Gastos del mes" y el resto de la app
      (presupuesto, patrimonio neto) siguen sumando el total real sin
      filtrar, ya que el usuario solo pidió el cambio para "el diagrama de
      torta" — esos otros lugares son un registro/control, no un indicador
      de avance de gasto.
    → "Total gastado y Total disponible siguen en 0, no se ha sincronizado":
      no era un problema de sincronización — "Total gastado" (arriba de
      Resumen) estaba sumando SOLO los gastos hechos con tarjeta de crédito
      (creditSpent), que da $0 si el usuario no ha cargado ningún gasto a una
      tarjeta de crédito todavía (confirmado en vivo en la tablet: mientras
      "Total gastado" mostraba $0, la tarjeta de "Presupuesto mensual" un
      poco más abajo, que sí usa el total real, mostraba correctamente
      "Gastado $98.000"). Se cambió "Total gastado" para usar el total real
      (totalSpent, todas las cuentas) en vez de solo crédito — confirmado en
      vivo que ahora coincide con "Presupuesto mensual". "Total disponible"
      no tenía este problema (ya mostraba $605.000 real, débito + efectivo)
      y se dejó igual.
    → Verificado con `npx tsc --noEmit` sin errores y en vivo en la tablet
      (Billetera general con monto completo, Quincena 1/2 con su total al
      final, Total gastado sincronizado con Presupuesto mensual). La
      exclusión de gastos recurrentes sin fecha vencida todavía (punto 3) se
      confirmó por revisión de código — reutiliza el mismo parseo de fecha
      "D/M/AAAA" ya probado en CardFormModal — ya que no había, en este
      momento, ningún gasto real con fecha de pago futura configurada para
      verlo en acción en pantalla.

  ✓ Quitada la tarjeta "Total deuda" redundante en Tarjetas (pedido en vivo,
    2026-07-21, FIXA)
    → En la pestaña Crédito de Tarjetas, la tarjeta naranja "Total deuda"
      (con el total y navegación a "Historial de abonos") quedó duplicada
      desde que se agregó el ícono de reloj junto al título de "Préstamos"
      (misma ronda anterior, ver "Toggle pequeño en cada sección..." más
      abajo): el encabezado "Préstamos" ya muestra título + total + ícono de
      reloj que abre el mismo "Historial de abonos". Se eliminó el bloque
      completo (debtSummaryCard y sus estilos, sin uso en otro lado).
    → Verificado con `npx tsc --noEmit` sin errores y en vivo en la tablet:
      la sección "Préstamos" sigue mostrando el total y abriendo el
      historial correctamente desde su ícono de reloj.

  ✓ Editar cualquier gasto desde "Gastos del mes" + tarjeta obligatoria
    (pedido en vivo, 2026-07-21, PECAS)
    → "En Gastos, en el pop up, deja editar cualquier gasto": el popup
      "Gastos del mes" (se abre tocando el donut de Gastos en Resumen) era
      de solo lectura. Ahora cada fila tiene un ícono de lápiz que abre un
      formulario de edición completo (nombre, monto, tarjeta, recurrente +
      frecuencia + fecha de pago) para CUALQUIER gasto del mes, sin
      importar su categoría — antes solo se podía editar entrando a
      Categorías → la categoría específica del gasto.
    → "Quita la leyenda de 2da quincena": mismo arreglo ya aplicado en
      Categorías — un gasto recurrente ahora muestra su propia frecuencia
      (Semanal/Quincenal/Mensual) en vez de "1ª/2ª Quincena" en este popup
      también (se había quedado sin corregir aquí en la ronda anterior).
    → "Que sea obligatorio elegir de dónde salió el dinero y que se vaya
      descontando": en este formulario de edición, a diferencia del de
      Categorías (donde la tarjeta quedó opcional a propósito), la tarjeta
      es OBLIGATORIA — no hay opción "Sin tarjeta", y "Guardar" queda
      deshabilitado con un aviso en rojo hasta que se elige una. El
      descuento del saldo de esa cuenta ya pasa automáticamente en cuanto
      se asigna: no fue necesaria lógica nueva, la app ya restaba el gasto
      del saldo disponible de cualquier tarjeta con cardId asignado
      (getCardTotalSpent), solo faltaba forzar que siempre hubiera una.
    → Nota: se probó en vivo editando el gasto real "Entretenimiento"
      (Tony) para verificar el flujo completo, y quedó con "Nequi"
      asignado como tarjeta — el "Total disponible" bajó de $655.000 a
      $625.000 al guardar, confirmando el descuento. A diferencia de otras
      pruebas de esta sesión, este cambio NO se revirtió: como la tarjeta
      ahora es obligatoria, ya no existe la opción de volver a dejarlo sin
      tarjeta desde la app — si "Nequi" no era la cuenta correcta para ese
      gasto, cámbiala manualmente tocando el lápiz de esa fila.
    → Verificado con `npx tsc --noEmit` sin errores y en vivo en la tablet.

  ✓ Quitado el monto gastado de las tarjetas de Quincena (pedido en vivo,
    2026-07-21, FIXA)
    → Cada slide de Quincena 1/2 ya no muestra "$X gastado" — se quita el
      Text y su estilo (quincenaCardSpent) y la función quincenaSpent()
      que quedó sin uso. Se conserva la barra de progreso (que mide avance
      de fechas dentro del periodo, no gasto) y el contador "X de Y gastos
      fijos pagados" agregado en la ronda anterior.
    → Verificado con `npx tsc --noEmit` sin errores y en vivo en la
      tablet. Nota aparte: durante la verificación se encontró que el
      túnel `adb reverse tcp:8081` se había caído en algún momento de la
      sesión (por eso la app mostraba "Failed to download remote update"
      al recargar) — se restableció con `adb reverse tcp:8081 tcp:8081`;
      no era un bug de código, solo de la conexión USB con la tablet.

  ✓ MEJORAX — sexta ronda de pendientes "por MI" del 2026-07-21 (FIXA)
    → "En el pop up de cada categoría, quita que diga siempre 2da
      Quincena": bug real, no solo percepción — como la quincena se asigna
      sola según la fecha de hoy (día 21, ya pasado el 15), CUALQUIER gasto
      anotado hoy caía en "2ª Quincena", y ese dato no significa nada para
      un gasto recurrente (que no vive "en una quincena", vive en su propia
      frecuencia semanal/quincenal/mensual). En la lista de gastos de
      CategoryDetailModal, un gasto recurrente ahora muestra su propia
      frecuencia (Semanal/Quincenal/Mensual) en vez de "1ª/2ª Quincena" —
      los gastos normales (no recurrentes) siguen mostrando la quincena
      igual que antes, ahí sí es un dato útil.
    → "Opcional: que un gasto recurrente diga qué día hay que pagarlo, con
      un calendario": nuevo campo recurrenceDueDate?: string en la
      interfaz Expense (storage.ts). En CategoryDetailModal, al activar
      "Gasto recurrente" aparece un campo opcional "¿Qué día lo tienes que
      pagar?" que abre el calendario nativo del celular (mismo patrón ya
      usado para la fecha límite de préstamos en CardFormModal). Si se
      llena, la lista de gastos lo muestra: "Mensual · vence 30/7/2026".
    → Verificado en la tablet con el gasto real "Comida": se activó
      temporalmente como recurrente mensual con fecha 30/7/2026 — la lista
      mostró "Mensual · vence 30/7/2026" correctamente — y se revirtió a su
      estado original (no recurrente, "2ª Quincena") al terminar la prueba,
      sin dejar cambios en los datos del usuario.
    → Verificado con `npx tsc --noEmit` sin errores.

  ✓ MEJORAX — quinta ronda de pendientes "por MI" del 2026-07-21: plan de
    gastos recurrentes + seguimiento de pagados (FIXA/PECAS)
    → "En el pop up de agregar una categoría, elimina Quincena y Tarjeta,
      que solo deje gasto recurrente y agrega una opción quincenal":
      CategoryDetailModal (agregar/editar gasto dentro de una categoría) ya
      no muestra los selectores de Quincena ni Tarjeta — la quincena se
      asigna sola según la fecha de hoy al crear el gasto (igual que ya
      hacía el registro rápido), y al editar se conserva la quincena/
      tarjeta que el gasto ya tenía (no se pierde el dato, solo se dejó de
      poder cambiar desde aquí). El toggle "Gasto recurrente" ahora incluye
      3 frecuencias: Semanal, Quincenal (nueva) y Mensual. Se agregó
      'biweekly' a RecurrenceFrequency (storage.ts) y su recordatorio en
      notifications.ts usa un trigger de intervalo de 15 días (expo-
      notifications no tiene un trigger nativo "quincenal").
    → "Que el diagrama de Gastos del mes solo muestre lo que ya completé":
      ya era así — el diagrama y su leyenda solo cuentan gastos
      efectivamente registrados, nunca gastos fijos pendientes de pagar.
      No requirió cambios, solo se confirmó y se explicó al usuario.
    → "Haz un plan para diferenciar gastos recurrentes de los no
      recurrentes y poder ir anotando cómo voy" + "en el pill de Quincena
      1 muestra los gastos recurrentes que aún faltan por pagar esa
      quincena": en vez de escribir código primero, se presentó el plan al
      usuario (diagnóstico de cómo funciona hoy "gasto recurrente" —solo
      agenda un recordatorio, sin seguimiento de pagado/pendiente— y qué se
      construiría encima de getRecurringTemplates()/"Pendientes por pagar"
      que ya existían). El usuario confirmó 2 decisiones antes de tocar
      código: (1) marcar un gasto fijo como pagado sigue siendo a través
      del popup "Pendientes por pagar" ya existente, sin pantalla nueva; (2)
      el conteo compara TODOS los gastos recurrentes registrados alguna vez
      contra lo ya anotado en cada quincena específica, no solo los
      creados originalmente en esa quincena.
    → Implementado con esas decisiones: cada slide de la tarjeta Quincena
      1/2 ahora muestra "X de Y gastos fijos pagados" (nueva función
      paidCountForQuincena(q) en resumen.tsx, reutilizando exactamente la
      misma comparación que ya usaba el popup "Pendientes por pagar" — sin
      lógica nueva duplicada). Tocar la tarjeta sigue abriendo ese mismo
      popup (ya era así). El texto no aparece si el usuario no tiene
      ningún gasto recurrente registrado todavía.
    → Verificado en la tablet: se creó un gasto recurrente de prueba
      ($150.000, mensual) — la Quincena 1 mostró "0 de 1" (el gasto cayó en
      la Quincena 2, que es la activa hoy 21 de julio) y la Quincena 2
      mostró "1 de 1", confirmando que el conteo es correcto por quincena.
      El gasto de prueba se eliminó al terminar (dato que YO creé, no del
      usuario). Verificado con `npx tsc --noEmit` sin errores.

  ✓ MEJORAX — cuarta ronda de pendientes "por MI" del 2026-07-21 (PECAS)
    → "En Categorías, leyenda en el + morado para que se sepa que crea una
      categoría nueva": agregada una pequeña pastilla "Nueva categoría"
      justo encima del botón "+" en app/categorias.tsx (mismo patrón visual
      ya usado en otros botones con leyenda de esta app).
    → "En el pop up de la categoría en tarjeta, agrega si quieres que sea
      recurrente o no": el usuario confirmó (se preguntó antes de tocar
      nada, por el riesgo de romper cálculos existentes) que era AGREGAR el
      toggle sin quitar Quincena ni Tarjeta. CategoryDetailModal (se abre
      al agregar/editar un gasto manual dentro de una categoría) ahora
      tiene el mismo toggle "Gasto recurrente" (+ Semanal/Mensual) que ya
      existía en el registro rápido (QuickEntryModal) — agenda o cancela el
      recordatorio correspondiente tanto al crear como al editar un gasto,
      incluyendo el caso de cambiar la frecuencia de un gasto que ya era
      recurrente.
    → "Que el diagrama de torta sea el primero en el desplazable": el
      usuario confirmó que se refería a mover "Billetera general" (el
      diagrama con TODAS las cuentas) al primer lugar del carrusel de
      diagramas en Resumen — antes iba último. Nuevo orden: Billetera
      general → Gastos del mes → Metas de ahorro → Fuentes de ingreso.
    → "Arregla el diagrama de Metas de ahorro en la tablet, no solo que se
      vean los emojis": la leyenda de este diagrama solo mostraba el emoji
      de cada meta + una barra de color, sin nombre ni monto ahorrado. Se
      reemplazó por el mismo componente de leyenda ya usado en Billetera
      general/Fuentes de ingreso (punto de color + nombre + monto), mismo
      arreglo de ancho que ya se corrigió para esos dos. goalsEmojiLegend/
      goalsEmojiItem/goalsEmojiChar/goalsEmojiBar eliminados (ya sin uso).
    → "Que el diagrama de Gastos haga el resumen de los gastos": ese
      diagrama no tenía ninguna leyenda debajo (a diferencia de los otros
      3). Se agregó una leyenda por categoría (punto de color + emoji/
      nombre + monto), usando el mismo catRows que ya alimentaba este
      donut — sin cálculos nuevos.
    → De paso, el usuario notó que el título "Julio 2026" del header
      (deslizable para ver meses anteriores, ya existía desde antes) no
      tenía ninguna pista visual de que se puede deslizar. Se agregaron
      dos flechitas (‹ ›) a los lados del título, atenuadas cuando no hay
      mes anterior/siguiente al que ir. Nota: en la tablet solo hay datos
      de julio, así que las flechas se ven pero no hay a dónde deslizar
      todavía — es esperado, no es un bug.
    → Bug encontrado y corregido de paso: al envolver el título del mes en
      un contenedor nuevo (para las flechas), el botón "❓" de ayuda quedó
      comprimido a una fracción de su tamaño normal — mismo tipo de bug de
      medición de ancho que ya había aparecido antes en esta sesión. Se
      corrigió dándole un ancho fijo explícito al contenedor del slider de
      meses en vez de dejar que se calculara solo.
    → Verificado con `npx tsc --noEmit` sin errores y probado en vivo en la
      tablet (no solo por código) para los 6 puntos de esta entrada.

  ✓ Corrección real del deslizable y de la leyenda — los arreglos de la
    ronda anterior NO eran suficientes (FIXA, 2026-07-21)
    → El usuario confirmó que, tras la 3ra ronda (ver más abajo), tanto el
      deslizable de Balance General como los montos de la leyenda de los
      donuts seguían sin funcionar. Se investigó de nuevo desde cero en
      lugar de repetir el mismo diagnóstico:
    → Deslizable de Balance General: la causa real no era el contentOffset
      (ya corregido antes) sino que cada slide entero (mes actual y mes
      anterior) estaba envuelto en un TouchableOpacity que cubría toda la
      tarjeta — cualquier intento de deslizar competía con ese botón por
      el gesto. El carrusel de donuts (que sí funcionaba) solo tiene el
      donut mismo dentro de un botón pequeño, no la tarjeta completa. Se
      cambió el envoltorio de cada slide a una View simple, dejando SOLO
      el texto "Toca para ver detalle →" como botón (con hitSlop para que
      siga siendo fácil de tocar). Confirmado con swipe manual en ambas
      direcciones en la tablet (mes anterior ↔ mes actual).
    → Montos cortados en la leyenda ("$500" en vez de "$500.000"): el
      intento anterior (minWidth + textAlign) no funcionaba porque el
      problema real estaba en cómo se repartía el espacio entre el nombre
      de la cuenta (flex:1) y el monto — en esta versión de React Native/
      Yoga, esa combinación terminaba midiendo mal el ancho del monto y
      lo dejaba con unos pocos píxeles de espacio real aunque el texto
      completo estuviera ahí. Se probaron varias alternativas (ancho fijo
      solo, justifyContent:space-between, marginLeft:auto) hasta encontrar
      la combinación que sí respeta el espacio: contenedor izquierdo
      (ícono + nombre) separado del monto, que ahora tiene marginLeft:auto
      + ancho fijo, sin numberOfLines ni textAlign:'right' (esas dos
      últimas, combinadas con el ancho fijo, volvían a cortar el texto).
      Confirmado visualmente en la tablet: "$500.000", "$50.000",
      "$36.000", "$69.000", "$25.000" se ven completos.
    → De paso, pedido nuevo en la misma conversación: la tarjeta "Gastos"
      de Resumen (accede a Categorías) se renombró a "Gastos fijos
      mensuales" y se reubicó justo después de la Quincena, antes de
      Balance General, agrupando todo lo relacionado a gasto/presupuesto
      del mes en una sola zona de la pantalla antes de pasar a patrimonio
      neto y metas.
    → También pedido en la misma conversación: la cápsula de registro
      (botón "+" de Resumen) ahora muestra una leyenda de texto (Ingreso /
      Gasto / Meta) encima de cada ícono para que sea claro qué hace cada
      botón.
    → Verificado con `npx tsc --noEmit` sin errores y probado en vivo en
      la tablet (no solo por código) para los 4 puntos de esta entrada.

  ✓ MEJORAX — tercera ronda de pendientes "por MI" del 2026-07-21 (PECAS)
    → "Selector de fecha para la fecha límite de pago en préstamos, no
      teclado": CardFormModal usaba un TextInput libre (DD/MM/AAAA). Se
      reemplazó por el mismo patrón ya usado en metas de ahorro
      (GoalFormModal): un campo tocable que abre el DateTimePicker nativo
      del celular. El valor se sigue guardando como "DD/M/AAAA" (formato
      por defecto de toLocaleDateString('es-CO')) para no romper
      parseDueDate(), que ya esperaba ese formato para agendar la
      notificación de vencimiento.
    → "Toggle pequeño en cada sección para ver el historial (crédito,
      débito, efectivo, préstamo)": se agregó un ícono de reloj junto al
      título de cada sección en Tarjetas. Débito/Efectivo abren un modal
      con los depósitos combinados de todas las cuentas de ese tipo
      (card.events tipo 'deposit'); Crédito abre un modal con los gastos
      cargados a cualquier tarjeta de crédito, combinados; Préstamos
      reutiliza el modal "Historial de abonos" que ya existía, solo se le
      agregó el mismo ícono para que sea consistente con las otras 3
      secciones (antes solo se abría tocando la tarjeta resumen "Total
      deuda").
    → "Suma el total en cada título, como en Préstamos": de las 4
      secciones, solo "Tarjetas de débito" no mostraba un total junto al
      título (Efectivo/Crédito/Préstamos ya lo tenían). Se agregó el saldo
      disponible total de débito (totalDebitAvail) en verde, mismo patrón
      que las demás.
    → "No puedo ver bien el diagrama de Billetera general en la tablet":
      encontrado con la accessibility tree del dispositivo (no solo
      mirando el código): el texto del monto en la leyenda de cada cuenta
      SÍ tenía el valor completo ("$500.000") pero su caja de layout medía
      apenas 43-57px de ancho — muy angosta para el texto, que quedaba
      visualmente cortado a "$500" aunque el dato real era correcto. La
      causa: incomeLegendName (flex:1) no tenía límite y le quitaba todo
      el espacio a incomeLegendAmt, que no tenía flexShrink ni minWidth.
      Se le agregó minWidth:92 + textAlign:'right' a incomeLegendAmt —
      esto también corrige la misma leyenda compartida en los donuts de
      Metas y Fuentes de ingreso, que iban a tener el mismo problema en
      cuanto tuvieran datos.
    → "Títulos de los diagramas arriba de cada uno, con letra más
      visible": los 4 títulos (Gastos del mes, Metas de ahorro, Fuentes de
      ingreso, Billetera general) se movieron de abajo del donut (con
      leyenda incluida) a arriba de todo el slide. heroDonutLabel pasó de
      color muted/fontSize sm a color de texto normal/fontSize md/peso 800.
    → Verificado en la tablet: selector de fecha nativo abre y guarda bien
      la fecha, toggle de historial de débito abre su modal
      correctamente (vacío, ya que estas cuentas se cargaron con saldo
      inicial y no con "Agregar dinero"), títulos arriba en los 4 slides
      confirmados. La leyenda de Billetera general se verificó por medición
      de la accessibility tree (ancho de caja subió de ~50px a 114px) — la
      imagen del screenshot en baja resolución no permite confirmarlo a
      simple vista, pero el dato de layout confirma el arreglo.
    → Verificado con `npx tsc --noEmit` sin errores.

  ✓ "Pon el título de las tarjetas de crédito" — corrección real (2026-07-21)
    → El primer intento (arreglar el solapamiento de "••••"/"Disponible" en
      CardView) no era lo que faltaba. El usuario confirmó que era el
      encabezado "Tarjetas de crédito": solo aparecía cuando ya había al
      menos 1 tarjeta creada — en el estado vacío (sin ninguna tarjeta
      todavía) el título no se mostraba, a diferencia de Efectivo/Préstamos
      que sí lo tenían fijo siempre. Se movió el título fuera del if/else
      en app/(tabs)/tarjetas.tsx para que se vea siempre, tanto en Débito
      como en Crédito, igual que las otras dos secciones.

  ✓ MEJORAX — segunda ronda de pendientes "por MI" del 2026-07-21 (PECAS)
    → "Pon el título de las tarjetas de crédito" (primer intento, incompleto
      — ver corrección arriba): el bug real detrás de este pedido era que
      components/CardView.tsx (después del límite de ancho agregado horas
      antes en la misma tablet) dejaba el texto "•••• •••• •••• 1234" y
      "Disponible $X" superpuestos/cortados en la parte de abajo de una
      tarjeta de crédito — no había espacio suficiente para ambos en el
      ancho ya limitado. Se acortó el número enmascarado a "•••• 1234" (en
      vez de repetir 4 grupos de puntos) y se le dio flex:1 + numberOfLines
      al contenedor izquierdo para que ceda espacio en vez de solaparse con
      "Disponible". Este arreglo seguía siendo válido, solo no era completo.
    → "Recuérdale el deslizable entre el mes actual y el anterior": el
      slider de "Balance General" (resumen.tsx) ya existía, pero usaba
      contentOffset={{x: cardWidth, y:0}} como prop declarativa — React
      Native la reevalúa en cada re-render (esta pantalla se re-renderiza
      seguido), así que cualquier actualización de estado cercana hacía que
      el slider "regresara" al mes actual, comiéndose el swipe del usuario.
      Se cambió al mismo patrón ya usado en el slider de meses del header
      (monthScrollRef): un ref + scrollTo() una sola vez con
      requestAnimationFrame, guardado por un flag (balanceScrollSynced) para
      no repetirlo en cada render.
    → "Arregla la pantalla de Historial, se ve muy fea": era una regresión
      de esta misma sesión — el ajuste de PECAS de horas antes le había
      puesto flexGrow+justifyContent:'center' al scroll de Historial para
      rellenar el espacio vacío en tablets, pero en una lista (a diferencia
      de una pantalla de onboarding) eso hace que la única tarjeta de mes
      quede flotando en la mitad de la pantalla, desconectada del
      encabezado. Revertido a top-anchored (solo padding, sin centrado).
    → "Que el diagrama de torta no muestre todas las transacciones sino que
      las clasifique por débito/crédito/efectivo": el modal "Gastos del mes"
      (se abre tocando el donut de Gastos en el hero de Resumen) mostraba
      todos los gastos del mes en una sola lista plana ordenada por fecha.
      Ahora se agrupan primero por tipo de cuenta (🏦 Débito · 💳 Crédito ·
      💵 Efectivo · 💸 Préstamos, en ese orden, solo los grupos con gastos),
      cada grupo con su propio subtotal, y dentro de cada grupo se mantiene
      el orden cronológico. Nueva constante ACCOUNT_TYPE_GROUPS.
    → Verificado en la tablet: tarjeta de crédito sin solapamiento, Gastos
      del mes agrupado, Historial vuelto a top-anchored. El swipe de
      Balance General no se pudo confirmar visualmente por una limitación
      de la herramienta de automatización (gestos horizontales dentro de un
      scroll vertical no siempre se detectan bien de forma sintética) — el
      fix en código replica exactamente el patrón que ya funciona en el
      slider de meses del header, así que se dejó así; si al probarlo a
      mano el swipe sigue sin sentirse bien, avisar para revisarlo de nuevo.
    → Verificado con `npx tsc --noEmit` sin errores.

  ✓ MEJORAX — ronda de pendientes "por MI" del 2026-07-21 (PECAS)
    → Tarjetas (Balance): quitados los botones con líneas punteadas de los
      estados vacíos (Débito/Efectivo/Crédito/Préstamos) y los "+" pequeños
      de cada encabezado de sección — ahora la única forma de agregar es el
      "+" grande de abajo a la derecha. Estilos muertos addDashedBtn/
      addDashedText/addMoreBtn/addMoreText/sectionAddBtn eliminados.
    → Ese mismo "+" grande, estando en la sub-pestaña Débito, ya abría el
      formulario dejando elegir entre Débito/Efectivo (y en Crédito entre
      Crédito/Préstamo) — comportamiento existente en openAdd(), no hubo que
      tocarlo, solo quitar los atajos redundantes de un solo tipo.
    → CardFormModal: cuando el formulario solo permite un tipo (por ejemplo
      al editar una tarjeta ya existente), se quitó el bloque "Tipo de
      cuenta" con la insignia bloqueada — no aporta nada que el título del
      formulario y la vista previa no digan ya. Sigue mostrándose el
      selector interactivo cuando hay 2 tipos para elegir.
    → CardFormModal: el campo de emoji ya no depende de un placeholder fijo
      (🏦/💳/💵/💸) — ahora muestra 4 emojis sugeridos según el tipo de
      cuenta (ej. efectivo: 💵💰👛🪙) como chips tocables + un campo "Otro"
      para escribir uno distinto.
    → CardFormModal: el saldo/límite ahora es obligatorio para guardar
      (Guardar queda deshabilitado sin monto, para débito/crédito/efectivo/
      préstamo). Si se intenta cancelar o cerrar el formulario habiendo
      puesto un alias pero sin monto, aparece un modal ("Falta el monto")
      con dos opciones: "Descartar" o "Dejar en $0" — mismo estilo visual
      que el resto de confirmaciones de la app.
    → Verificado en la tablet: flujo completo de crear/cancelar/dejar en $0
      probado en vivo, y `npx tsc --noEmit` sin errores.
    → "Balance General" con deslizable mes actual/anterior: ya existía
      (implementado en rondas previas, ver más abajo) — se confirmó que
      sigue funcionando, no requirió cambios nuevos.
    → Pendiente de avatar en vez de foto de perfil: quedó sin implementar
      a propósito — la app ya usa avatar por emoji (no hay foto de perfil
      que reemplazar) y el pedido venía con "quizá", así que se dejó como
      idea para retomar si se confirma que se quiere de verdad.

  ✓ EAS ya estaba vinculado — nota desactualizada (AUDITA, 2026-07-21)
    → El pendiente "por MAQUINA" de hacer eas login/eas init ya no aplicaba: app.json
      tiene extra.eas.projectId configurado y eas.json ya tiene los 3 perfiles de build
      (development/preview/production). eas whoami confirmó sesión activa (nicksero) en
      esta máquina. Se quitó el pendiente de este archivo.

  ✓ "Eliminar aporte" (meta) y "Cerrar sesión" ya no rompen el tema oscuro (FIXA, 2026-07-21)
    → Seguimiento de la auditoría AUDITA del mismo día: quedaban 2 lugares con el mismo
      Alert.alert nativo que ya se había reemplazado en el resto de la app.
    → app/(tabs)/resumen.tsx (GoalDetailModal): nuevo estado confirmDeleteDeposit. El
      long-press sobre un aporte del historial de una meta ya no llama Alert.alert —
      abre un modal de confirmación centrado (ícono de basurero, botones Cancelar/
      Eliminar), mismo estilo visual que "Eliminar meta" (estilos duplicados como
      confirm* dentro de gdStyles, ya que GoalDetailModal es un componente separado
      con sus propios estilos).
    → app/(tabs)/perfil.tsx: nuevo estado confirmLogout + modal de confirmación
      centrado con el mismo patrón visual (ícono log-out, texto, Cancelar/Cerrar
      sesión). Se quitó el import de Alert (ya sin uso en el archivo).
    → Verificado con `npx tsc --noEmit` sin errores en ambos archivos.

  ✓ "Eliminar" ya no rompe el tema oscuro en Metas ni en Tarjetas (FIXA/ESTETIK, 2026-07-19)
    → Seguimiento del informe ESTETIK "fluidez e interacción" (12 jul 2026): el hallazgo
      #2 original solo cubría "Eliminar gasto" en categorías; al revisar qué tan
      extendido estaba el patrón, se encontró el mismo Alert.alert nativo (blanco/negro,
      rompe el tema oscuro) en dos lugares más: Metas de ahorro y Tarjetas/Cuentas.
    → app/(tabs)/resumen.tsx: nuevos estados actionGoal/confirmDeleteGoal. El long-press
      sobre una meta ahora abre un bottom sheet oscuro (Editar meta / Eliminar) en vez
      de Alert.alert — mismo patrón ya usado en categorias.tsx (menú de acciones de
      categoría). handleDeleteGoal ya no llama Alert.alert: abre un modal de
      confirmación centrado (ícono de basurero, texto, botones Cancelar/Eliminar en
      rojo), reutilizando el mismo estilo visual de "Eliminar categoría".
    → app/(tabs)/tarjetas.tsx: el botón "Eliminar" del panel de acción (que ya era un
      bottom sheet a medida, eso no cambió) ya no dispara Alert.alert al confirmar —
      nuevo estado confirmDeleteCard + modal de confirmación centrado, mismo estilo
      visual que el "Modal de abono excedido" que ya existía en este archivo
      (overflowModal/overflowCard), para no introducir un cuarto patrón visual distinto.
      Se quitó el import de Alert (ya sin uso en el archivo).
    → Verificado con `npx tsc --noEmit` sin errores en ambos archivos.

  ✓ 5 cambios de la sesión del 2026-07-12, verificados y documentados
    en v1.16 (2026-07-15)
    → Quedaron en el código (commits de auto-sync) pero sin registrar en
      ACTUALIZACIONES.txt ni acá — se probaron uno por uno en el emulador
      antes de darlos por buenos:
    → Grid de categorías dentro de QuickEntryModal al registrar un gasto
    → Sección "Primeros pasos" en Resumen, solo visible sin datos aún
    → DateTimePicker nativo para "Fecha límite" en metas de ahorro
      (nueva dependencia @react-native-community/datetimepicker)
    → Modal temático de confirmación al eliminar gasto (CategoryDetailModal),
      reemplaza el Alert.alert nativo
    → Botón "+" de Tarjetas de crédito en color COLORS.debt (naranja)

  ✓ MEJORAX — pendiente "por MI", botón "+" en sección Efectivo (FIXA, 2026-07-08)
    → Tarjetas (Balance) → sub-pestaña Débito → sección "Efectivo": se quitó
      el texto naranja "+ Agregar efectivo" que estaba debajo de la lista de
      cuentas de efectivo. En su lugar, el encabezado "Efectivo" ahora tiene
      el mismo botón "+" circular (mismo estilo/patrón que ya tenían
      "Tarjetas de débito" y "Tarjetas de crédito") — solo visible cuando ya
      hay al menos una cuenta de efectivo. El estado vacío (sin ninguna
      cuenta de efectivo todavía) no se tocó: sigue con el botón punteado
      "+ Agregar efectivo" grande, igual que el resto de secciones vacías.

  ✓ MEJORAX — pendiente "por MI", botón Registrar en cápsula (FIXA, 2026-07-08)
    → El botón "+" (Registrar) de Resumen ya no abre un bottom sheet de lista
      completa ("¿Qué quieres registrar?" con 2 filas). Ahora, al tocarlo, el
      mismo FAB se transforma en una cápsula redondeada flotante (mismo
      estilo que "la foto" de referencia del usuario: pastilla oscura con
      botones circulares + una "X" para cerrar) con 2 botones — 💸 Gasto/
      Ingreso y 🎯 Nueva meta de ahorro — y una "X" al final para cerrar sin
      elegir nada. Se confirmó que este era el único lugar de la app con
      este patrón de botón (no había que replicarlo en otra pantalla).
    → Se quitó el Modal con overlay oscuro que tenía antes (no aplica en el
      diseño de cápsula, que flota sin oscurecer el fondo). Estilos muertos
      regOption/regOptionEmoji eliminados; regOptionTitle/regOptionSub se
      conservan porque los sigue usando el sheet de Ayuda (PDF/Extracto/❓).

  ✓ MEJORAX — pendiente "por MI" (FIXA, 2026-07-08)
    → Pantalla Tarjetas (Balance): se quitaron los recuadros "Total gastado"
      / "Total disponible" que aparecían arriba de la lista de tarjetas
      (tanto en la sub-pestaña Débito como en Crédito). En su lugar, el
      encabezado de cada sección ("Tarjetas de débito" / "Tarjetas de
      crédito") ahora tiene un botón "+" circular a la derecha para agregar
      tarjeta directamente ahí — se quitó el link de texto duplicado
      "Agregar débito"/"Agregar tarjeta" que estaba debajo de la lista.
    → Pantalla Resumen: se agregó el mismo par de recuadros "Total gastado"
      (gasto total en tarjetas de crédito) / "Total disponible" (débito +
      efectivo disponible) justo debajo del donut principal, reutilizando
      variables ya calculadas en la pantalla (creditSpent, debitAvailable,
      cashAvailable) — sin lógica nueva duplicada.

  ✓ Ajustes en vivo tras la 7ma ronda (PECAS, 2026-07-06)
    → "Gastos del mes" (tap en el donut de Gastos) dejó de abrir el popup
      combinado "Resumen del mes" — ahora abre uno propio e independiente
      con solo el listado de gastos del mes, en tarjetas (ícono de la
      categoría + nombre + quincena/tarjeta + monto), no en filas de lista
      plana. El popup "Resumen del mes" (sigue accesible desde "Presupuesto
      mensual") ya no repite ese listado.
    → Fix del slider de meses del header ("Julio 2026"): el contentOffset
      + remount por key causaba un parpadeo/glitch al cargar. Se reemplazó
      por un ScrollView con ref + scrollTo(), sincronizado UNA sola vez
      cuando llega la lista real de meses (useEffect con guard), sin pelear
      con el swipe del usuario en recargas posteriores. También se agregó
      numberOfLines+adjustsFontSizeToFit al texto del mes por si el nombre
      es más largo que el ancho fijo del slide (ej. "Septiembre 2026").

  ✓ MEJORAX de pendientes "por MI", 7ma ronda (PECAS, 2026-07-06)
    → Resumen ahora deja ver meses anteriores: el título "Julio 2026" del
      header es un slider deslizable (mismo patrón de los otros sliders)
      con todos los meses que tienen datos registrados + el mes actual.
      Deslizar cambia monthKey/prevMonthKey de toda la pantalla. Los
      efectos secundarios (snapshot de tarjetas, notificación de saldo,
      aviso de presupuesto 80/100%) quedaron protegidos con un chequeo
      isCurrentMonth para que NO se disparen al navegar por meses pasados.
    → Popup "Resumen del mes": nueva sección "📅 Balance general por mes"
      al final, con el neto (ingresos - gastos) de cada mes registrado.
    → Donut de "Balance por tarjeta" renombrado a "Billetera general" +
      chips de filtro (Todos/Débito/Crédito/Efectivo) arriba de la leyenda
      para ver solo un tipo de cuenta a la vez en el donut.
    → Donut de Metas de ahorro: el número del centro ya no se abrevia
      (fmtShort → formatCOP), muestra el monto completo — el propio
      DonutChart ya reduce el tamaño de fuente automáticamente si no cabe.
    → Cada tarjeta/modal de "Balance General" ahora incluye el mes
      correspondiente en el título (antes solo la del mes anterior lo tenía).

  ✓ MEJORAX de pendientes "por MI", 6ta ronda (PECAS, 2026-07-06)
    → Balance General: se invirtió el orden de los slides (mes anterior
      primero, mes actual segundo) pero la vista inicial queda "bloqueada"
      en mes actual vía contentOffset del ScrollView — al abrir Resumen se
      ve el mes actual igual que antes, y ahora hay que deslizar hacia la
      IZQUIERDA para ver el mes anterior (antes era al revés).
    → Se eliminaron las 2 cards de Débito/Crédito (el resumen gastado/
      disponible que estaba debajo del Balance General). En su lugar se
      agregó un 4to slide "Balance por cuenta" en el carrusel de diagramas
      de torta del hero: un donut con una porción por cada cuenta (débito/
      efectivo = disponible, crédito/deuda = gastado), coloreado con el
      color real de cada tarjeta (Card.color), con leyenda debajo y toque
      para abrir el mismo modal de detalle de Balance General.
    → Nuevo: lib/storage.ts guarda ahora un snapshot del estado de las
      tarjetas (saldo/límite) por cada mes (syncCardBalanceSnapshot /
      getCardBalanceSnapshot) — se sobreescribe mientras ese mes sigue
      siendo el actual y se "congela" solo cuando el mes cambia. El
      Balance General del "mes anterior" ahora usa ese snapshot en vez de
      los saldos actuales cuando existe; si no existe (meses previos a
      este cambio), cae de vuelta a la aproximación de antes.
    → El popup "Resumen del mes" (se abre al tocar la torta de Gastos)
      ahora incluye una sección nueva "🧾 Todos los gastos" con cada gasto
      individual del mes (no solo agrupado por categoría), ordenados del
      más reciente al más antiguo.
    → "Presupuesto mensual" en Resumen (dentro del hero, junto a los
      donuts) pasó a ser de solo lectura: tocarlo ahora abre el popup
      "Resumen del mes" en vez del formulario de edición. La edición del
      monto del presupuesto se movió a Perfil (nueva sección "Presupuesto
      mensual", mismo patrón que Periodicidad de cuenta).

  ✓ Categorías movidas a pantalla independiente (PECAS, 2026-07-06)
    → Pedido en vivo (no venía del archivo): unificar la sección "Categorías"
      de Resumen, que ocupaba mucho espacio con el grid/list completo.
    → Se creó app/categorias.tsx (pantalla nueva, empujada, con su propio
      header y botón atrás — mismo patrón que app/busqueda.tsx) con todo el
      grid/list de categorías, el botón "+" para crear, editar/eliminar por
      long-press, y el detalle de gastos por categoría. Registrada la ruta
      en app/_layout.tsx.
    → components/CategoryDetailModal.tsx extraído a su propio archivo
      (antes vivía inline al final de resumen.tsx) para poder reutilizarlo
      en la nueva pantalla sin duplicar ~250 líneas de código.
    → En Resumen, toda esa sección se reemplazó por una sola card compacta
      "Gastos" (ícono + total del mes + cantidad de categorías) que al
      tocarla navega a /categorias. catRows/catBreakdown se quedan en
      resumen.tsx porque el popup de "Resumen del mes" (donut de Gastos)
      todavía los necesita para su propio desglose por categoría.

  ✓ MEJORAX de pendientes "por MI", 5ta ronda (PECAS, 2026-07-06)
    → "Presupuesto mensual" se movió de su propia tarjeta a dentro del
      heroCard (sección de los diagramas de torta), justo debajo de los
      puntos de paginación del slider de donuts.
    → "Patrimonio Neto" renombrado a "Balance General" en toda la app
      (card de Resumen, modal de detalle, notificación de saldo en
      Perfil/lib/notifications.ts). La card ahora es un slider de 2
      slides: mes actual (toca → modal de detalle de activos/pasivos,
      como antes) y mes anterior (toca → popup de tendencia), reemplazando
      la tarjeta separada "vs. mes anterior" que existía debajo. El mes
      anterior es una aproximación: usa los gastos de ese mes contra los
      saldos ACTUALES de las cuentas, porque Card.balance/limit no guarda
      historial mes a mes — está bien para ver la tendencia, no es un
      corte contable exacto de cómo estaban las cuentas en ese momento.

  ✓ MEJORAX de pendientes "por MI", 4ta ronda (PECAS, 2026-07-06)
    → Ejemplos de Finando (app/(tabs)/index.tsx) ahora priorizan el gasto
      fijo/recurrente del usuario y, si no tiene ninguno, el último gasto
      real que registró este mes (nuevo estado recentExpense, derivado de
      getMonthData ya cargado en el mismo useFocusEffect) — antes del
      último ajuste solo se fijaba en gastos recurrentes, dejando el
      ejemplo genérico para cualquiera que no hubiera usado ese toggle.
    → Tarjeta de periodo quincenal ahora es un slider (igual patrón que el
      de los donuts): Quincena 1 y Quincena 2 como slides deslizables, cada
      una mostrando su propio gasto acumulado y badge "ACTIVA" en la que
      corresponde a hoy. Semanal y mensual siguen con tarjeta única (no
      aplica el concepto de "quincena 2"). El popup de "Pendientes por
      pagar" ahora responde a la quincena que se está mirando en el
      slider (viewedQuincena), no siempre a la de hoy.
    → Nueva sección "Periodicidad de cuenta" en Perfil (mismo patrón visual
      que el selector de Tema): el usuario ya puede cambiar entre
      semanal/quincenal/mensual en cualquier momento, no solo una vez en
      el onboarding — Resumen recarga el perfil cada vez que la pantalla
      gana foco, así que el cambio se refleja de inmediato al volver a esa
      pestaña.

  ✓ MEJORAX de pendientes "por MI", 3ra ronda (PECAS, 2026-07-06)
    → Donuts de Metas e Ingresos ahora son tappables igual que el de Gastos
      (resumen.tsx): cada uno abre su propio popup — metasModal lista cada
      meta con ahorrado/objetivo/%, ingresosModal lista cada fuente de
      ingreso con su monto. Reutilizan los estilos pat*/regSheet que ya
      existían para el modal de Patrimonio Neto.
    → Tarjeta de periodo (Quincena/Semana/Mes, según profile.budgetPeriod)
      ahora es tappable y abre "Pendientes por pagar": compara los gastos
      recurrentes que el usuario ha registrado alguna vez (nueva función
      getRecurringTemplates() en storage.ts, dedupe por nombre) contra los
      gastos ya registrados en la ventana del periodo activo (quincena 1/2
      por el campo quincena, semana actual por createdAt >= lunes, o todo
      el mes si es mensual) — lista los que faltan por registrar. Si el
      usuario no tiene gastos fijos configurados, o si ya está al día, lo
      indica explícitamente en vez de mostrar una lista vacía sin contexto.
    → Card "vs. mes anterior" ahora es tappable y abre un popup con
      LineChart (react-native-chart-kit, misma librería y estilo que el
      gráfico de tendencia de Historial) mostrando ingresos/gastos/ahorro
      de los últimos 2 meses (mes actual + anterior).
    → De paso (pedido en el mismo momento, mismo tema de "excel de base"):
      en app/(tabs)/index.tsx, el mensaje de bienvenida y los 3 chips de
      sugerencia del chat de Finando ya no muestran el ejemplo fijo
      "$475.000"/sueldo "$2.000.000" igual para todas las cuentas — ahora
      se arman con los datos reales de esta cuenta (gasto/ingreso fijo del
      onboarding vía getRecurringTemplates(), o la primera categoría propia
      si aún no configuró ninguno) y saludan por el nickname si existe.

  ✓ Fix de código para "Confirm email" de Supabase (PECAS, 2026-07-06)
    → app/onboarding.tsx handleRegister: signUp() ahora manda nickname/
      avatar_color/avatar_emoji en options.data (user_metadata de Supabase
      Auth, disponible desde el signUp aunque el correo no esté confirmado
      todavía). Si signUp() no devuelve sesión (correo sin confirmar), ya
      NO intenta insertar en profiles (eso era lo que disparaba el error de
      RLS) — en cambio avisa "Confirma tu correo" y manda a la pantalla de
      login.
    → handleLogin: la consulta a profiles cambió de .single() a
      .maybeSingle(). Si no existe fila (primer login tras confirmar el
      correo), la crea recién ahí usando los datos guardados en
      user_metadata — en ese punto ya hay sesión real con auth.uid()
      válido, así que la política RLS "insert own profile" la permite.
    → Con esto ya se puede reactivar "Confirm email" en el dashboard de
      Supabase cuando se quiera (ver pendiente "por MAQUINA" arriba, es
      solo un toggle, no requiere más código).

  ✓ MEJORAX de pendientes "por MI", 2da ronda (PECAS, 2026-07-06)
    → Finando ya no usa una base genérica compartida ("el Excel") para
      todos los usuarios: constants/systemPrompt.ts ahora recibe la lista
      real de categorías del usuario (buildSystemPrompt(nickname,
      categories)) y arma el prompt con sus ids reales en vez de la lista
      fija de categorías por defecto ("vivienda","comida",...). Se quitó
      además el ejemplo con montos fijos (Arriendo 475.000, Spotify 30.000,
      Sueldo 2.000.000) que era el mismo para toda cuenta — ahora la
      instrucción pide montos reales sin copiar valores de ejemplo. Se
      actualizó el call site en app/(tabs)/index.tsx para pasar
      `categories` (ya se cargaba en ese screen) a askAdvisor() → lib/claude.ts.
      El modo demo (usuarios anónimos sin sesión) queda igual a propósito,
      ya que por definición no hay cuenta que personalizar.
    → "Extracto bancario" renombrado a "Extracto de tu cuenta" en el botón
      de Resumen, el FAQ de ayuda.tsx y los títulos/pie del PDF en
      lib/financialReport.ts.
    → Aislamiento de datos por cuenta: lib/storage.ts ahora resuelve un
      namespace (profile.id, o 'anon' en modo anónimo) y lo agrega a las
      claves de AsyncStorage de tarjetas/categorías/metas/gastos-ingresos
      (getActiveNamespace() + K_CARDS/K_CATEGORIES/K_GOALS/K_EXP ya no son
      claves fijas, son funciones que reciben el namespace). Se migran
      (copian, sin borrar) los datos guardados antes de este cambio hacia
      el namespace del perfil activo la primera vez que se detectan, una
      sola vez por namespace (flag @wc_migrated_<ns>). Diseño deliberado:
      no se borra nada al cambiar de cuenta — cada cuenta ve solo su propio
      balde de datos en este dispositivo, y si se vuelve a esa cuenta más
      adelante su información sigue ahí intacta. El perfil de autenticación
      (wc_profile) sigue siendo una sola clave global, sin namespace (solo
      hay un perfil activo a la vez).

  ✓ Fase 3: saldo, búsqueda, comparativa + fix de responsive (PECAS, 2026-07-06)
    → Widget de saldo rápido: se confirmó que un widget nativo real de
      pantalla de inicio no es viable sin salir de Expo Go (requeriría
      react-native-android-widget + expo prebuild + un build nativo real).
      Se reemplazó por una notificación local opt-in (toggle en Perfil,
      apagado por defecto): lib/storage.ts (getShowBalanceNotification/
      saveShowBalanceNotification, computeNetWorth), lib/notifications.ts
      (updateBalanceNotification con identifier fijo 'wc-balance' para
      actualizar in-place en vez de apilar, cancelBalanceNotification).
    → Búsqueda de gastos: searchExpenses() en storage.ts (nombre, categoría,
      rango de fechas sobre todos los meses); pantalla nueva app/busqueda.tsx
      accesible desde la lupa en Historial.
    → Comparativa mes a mes: getPreviousMonthKey() en storage.ts; nueva card
      "vs. mes anterior" en Resumen (flecha + % de diferencia), oculta si
      el mes anterior no tiene gastos registrados.
    → Fix de responsive reportado por el usuario (celular del hermano):
      los montos grandes (pills Ingresos/Gastos, donut central, Débito/
      Crédito, Patrimonio Neto, popup de resumen, resultados de búsqueda)
      se partían en dos líneas en pantallas/fuentes distintas — se agregó
      adjustsFontSizeToFit + numberOfLines={1} a todos esos Text.
    → Auditoría final: quedan 2 hallazgos reales pendientes (aviso de
      permiso de notificaciones denegado en CardFormModal.tsx, y falta de
      accessibilityLabel/Role en toda la app) — documentados en el PDF
      actualizado junto con el plan final de publicación.

  ✓ MEJORAX de pendientes "por MI" (PECAS, 2026-07-06)
    → Formato de miles automático en todos los inputs numéricos de la app:
      nuevo helper formatThousands() en lib/expenseParser.ts, aplicado en
      QuickEntryModal (monto principal, se quitó el preview duplicado que
      ya no hacía falta), CategoryDetailModal y GoalDetailModal (resumen.tsx),
      y los campos de ingreso/gasto fijo del onboarding. Los inputs que ya
      formateaban (CardFormModal, BudgetFormModal, GoalFormModal, tarjetas.tsx)
      quedaron intactos.
    → Step "¿Cuánto gastas en cosas fijas?" del onboarding rediseñado: en vez
      de un solo monto, ahora es una lista — chips de sugerencias (Arriendo,
      Servicios, Internet/Celular, Suscripciones, Transporte, Seguro), campo
      de nombre + monto y botón "+" para ir agregando varios gastos fijos,
      cada uno con su propia tarjeta (nombre, monto, botón de eliminar). Al
      finalizar, cada ítem de la lista se crea como Expense recurrente
      mensual independiente (con su propio recordatorio), no un solo gasto
      combinado como antes.
    → Contraseña en onboarding: ojito para mostrar/ocultar en los campos de
      login y de crear cuenta (Ionicons eye-outline/eye-off-outline dentro
      de un TouchableOpacity, reutilizando el patrón inputWrap/inputIcon ya
      usado para el check del email). En el campo de crear cuenta se agregó
      además un medidor visual de fuerza (Débil/Media/Fuerte, barra de color
      rojo/dorado/verde) vía getPasswordStrength() — heurística simple por
      longitud + mayúsculas + números + símbolos, sin librería externa.

  ✓ Notificaciones de presupuesto + gastos recurrentes (PECAS, 2026-07-06)
    → lib/notifications.ts: helpers centralizados de expo-notifications
      (checkBudgetThreshold, scheduleRecurringReminder, cancelNotification)
    → storage.ts: Expense gana isRecurring/recurrenceFrequency/notificationId;
      MonthData gana budgetNotified (watermark 0/80/100 para no repetir alertas)
    → resumen.tsx: BudgetProgressBar montado (antes existía huérfano, sin usar)
      + BudgetFormModal nuevo para fijar el presupuesto mensual; al tocar la
      barra o el estado vacío se abre el modal; load() revisa el umbral en
      cada carga y dispara notificación local al 80% y al 100%
    → QuickEntryModal: toggle "Gasto recurrente" (semanal/mensual) al
      registrar un gasto; agenda recordatorio a las 9AM con trigger WEEKLY/
      MONTHLY de expo-notifications; se cancela automáticamente al borrar
      el gasto desde CategoryDetailModal

  ✓ Selector de emoji: recomendados al tocar el círculo (MEJORAX, 2do ajuste)
    → Limitación confirmada con el usuario: no existe API en Android/iOS para
      forzar que se abra el panel de emojis del teclado nativo directamente
      (ni WhatsApp/Instagram pueden hacerlo) — se descartó esa ruta
    → Solución acordada: tocar el círculo del avatar despliega una fila
      compacta de 8 emojis recomendados (💵😀😎🚀🐱⭐🔥🎯), no un grid
      permanente — aparece solo al tocar y se cierra al elegir uno
    → Botón "lápiz" al final de la fila enfoca un TextInput oculto para
      quien quiera escribir un emoji distinto desde su propio teclado
      (sigue filtrado con \p{Extended_Pictographic}, solo acepta emoji)
    → avatarEmoji sigue arrancando en '💵' por defecto

  ✓ Login con correo o nombre de usuario (MEJORAX)
    → Se agregó columna `email` a la tabla `profiles` + función RPC
      `get_email_by_nickname` (security definer) para resolver username→email
      sin exponer el resto de la fila (RLS de profiles sigue intacta)
    → handleRegister ahora guarda el email en profiles al crear la cuenta
    → handleLogin detecta si el texto ingresado tiene "@" (correo) o no
      (nombre de usuario) y resuelve el email real antes de llamar
      signInWithPassword
    → Input de login renombrado a "Correo o nombre de usuario"

  ✓ Emoji obligatorio en Crear cuenta (MEJORAX)
    → Eliminado el fallback a iniciales; avatarEmoji ahora es requerido en canRegister
    → Label cambiado a "Elige un emoji (obligatorio):" — sigue usando el teclado
      nativo de emojis del celular vía TextInput simple, sin picker propio

  ✓ Quitado "Nombre completo", nickname pasa a ser el nombre de usuario (MEJORAX)
    → Eliminado el input y el state `name` de app/onboarding.tsx
    → El campo nickname ahora se etiqueta "¿Cómo quieres que te llame Finando?
      (tu nombre de usuario)" y se guarda tanto en `name` como en `nickname`
      (perfiles de Supabase y perfil local)
    → Pantalla "done" saluda con el nickname en vez del nombre completo

  ✓ Responsive de onboarding — welcome y choice ahora scrollean (MEJORAX)
    → Los pasos 'welcome' y 'choice' antes usaban View fijo (podían recortarse
      en pantallas chicas); ahora están envueltos en ScrollView con
      contentContainerStyle flexGrow:1, igual que register/login

  ✓ Pills Ingresos/Gastos con tipografía elegante (v1.12)
    → Rediseñadas como tarjetas verticales con fondo sólido (verde/rojo)
    → Label en uppercase pequeño arriba · valor grande en blanco abajo
    → Sin corte de texto · legible y llamativo en cualquier monto

  ✓ Leyenda de metas vacías en donut (v1.12)
    → DonutChart ahora acepta emptyLabel y emptyHint como props
    → Slide de Metas muestra "Sin metas aún · Toca + para crear tu primera meta de ahorro"
    → Slide de Ingresos muestra "Sin ingresos · Toca + para registrar un ingreso"

  ✓ Diagrama de torta para Ingresos (v1.12)
    → 3er slide en el gadget swipeable: donut de fuentes de ingreso
    → Cada ingreso registrado = una porción con color de INCOME_COLORS
    → Centro muestra total de ingresos del mes
    → Lista de hasta 4 ingresos con punto de color, nombre y monto debajo

  ✓ Emojis y valor total en donut de Metas (v1.12)
    → Centro del donut muestra totalSaved/totalTarget en formato compacto ($500k / de $2.0M)
    → Leyenda con emoji de cada meta + barra de su color debajo del donut
    → fmtShort() helper para comprimir valores grandes en el centro del donut

  ✓ FAB con menú Registrar (v1.11)
    → Botón flotante abre registrarSheet con 2 opciones: Gasto/Ingreso + Meta
    → Botón "Meta" eliminado del header
    → onPress del FAB cambiado de setQuickEntry a setRegistrarSheet

  ✓ Donut gadget swipeable (v1.11)
    → heroCard usa ScrollView horizontal pagingEnabled en lugar de dos donuts lado a lado
    → Slide 1: donut de gastos (tappable → summaryModal)
    → Slide 2: donut de metas (goalsDonutData)
    → Dots de paginación que se extienden según la página activa
    → Pills Ingresos/Gastos movidas ARRIBA del slider (antes estaban abajo)

  ✓ Botón ❓ único en header (v1.11)
    → Reemplaza 3 botones (PDF + Ayuda + Meta) por un solo ❓
    → helpSheet con opciones 📊 PDF y ❓ Ayuda

  ✓ Patrimonio Neto tappable (v1.11)
    → TouchableOpacity envuelve el card, onPress abre patrimonioModal
    → Modal con desglose de Activos (debit + cash) y Pasivos (credit + debt)
    → Totales por sección + PatNetBox con color reactivo al neto
    → activoItems / pasivoItems computados desde cards[]

  ✓ CategoryFormModal más llamativo (v1.11)
    → Hero header coloreado con el color auto-asignado de la categoría
    → Emoji grande (52px) + nombre + subtítulo contextual en tiempo real
    → Botón Guardar toma el mismo color del hero
    → Labels en uppercase, input con borderWidth 1.5 y padding más generoso

  ✓ Dos donuts lado a lado en el hero (v1.10)
    → Reemplazado el donut único centrado por dos donuts en fila
    → goalsDonutData: goals.filter(savedAmount > 0).map → DonutSlice por meta
    → donutSizeSm = min((SCREEN_W - 80) / 2, 150) para ambos donuts
    → Goals donut: total=totalTarget, muestra el arco vacío como lo que falta
    → Center del donut de metas: "🎯 Metas" + porcentaje global
    → Si no hay metas: "Sin metas" en el centro del donut vacío
    → Separador vertical View entre los dos donuts
    → Eliminado el heroGoalsWidget de barra de progreso (reemplazado por el donut)

  ✓ Título del header solo con el mes (v1.10)
    → Removida la línea <Text>Resumen</Text>
    → headerTitle ahora muestra formatMonthLabel(monthKey) en negrita
    → headerSub eliminado

  ✓ FAB label "Registrar" más llamativo (v1.10)
    → fontSize 10 → 12, backgroundColor: COLORS.primaryBg (pill suave)
    → paddingHorizontal 10, borderRadius 10, border fino en primary + '33'
    → Efecto de badge/pill sin ser intrusivo

  ✓ Formateo de miles en campos de metas (v1.9)
    → TextInput "Monto objetivo" y "Ya ahorrado" en GoalFormModal
    → value={target ? Number(target).toLocaleString('es-CO') : ''}
    → onChangeText sigue stripping con /\D/g — no rompe el cálculo

  ✓ Gadget de metas dentro del hero card (v1.9)
    → Aparece debajo de las pills si goals.length > 0 && totalTarget > 0
    → Barra de progreso horizontal con % global de metas
    → "🎯 Metas" label + {totalSaved} ahorrados / de {totalTarget}

  ✓ Botones PDF/Ayuda/Meta más llamativos (v1.9)
    → Reemplazados Ionicons por emojis: 📊 PDF · ❓ Ayuda · 🎯 Meta
    → Padding y border más grandes, fontWeight 800
    → Meta ahora tiene fondo sólido (headerBtnPrimary) para destacar

  ✓ Popup Resumen del mes rediseñado (v1.9)
    → Fix gap: estructura cambiada a View+TouchableOpacity-dismiss+View sheet
    → 3 stat boxes: Ingresos 💰 · Gastos 💸 · Balance 📈/📉
    → Sección "Gastos por categoría" con header, importes coloreados y mini-bars
    → Sección "Fuentes de ingreso" con lista de incomes si existen

  ✓ Emoji del usuario en GoalDetailModal (v1.9)
    → goal?.emoji ?? (done ? '🎉' : '🎯') — muestra el emoji elegido
    → Sin emoji: fallback a 🎉 completada / 🎯 en progreso

  ✓ Suma global de metas en sección Metas de ahorro (v1.8)
    → Eliminado el resumen 2x2 (goalsSummaryCard) que estaba debajo de Patrimonio Neto
    → Nuevo bloque inline encima del listado: total ahorrado (verde) + "de X · Y%"
    → Barra de progreso global que resume el avance de todas las metas
    → Solo aparece cuando goals.length > 0 && totalTarget > 0

  ✓ Emoji picker en formulario de metas de ahorro (v1.8)
    → Grid de 30 emojis (igual al de categorías) reemplaza el selector de colores
    → Color de la barra se auto-asigna según posición del emoji (GOAL_COLORS)
    → Preview muestra emoji + nombre de la meta en tiempo real
    → Goal.emoji?: string ya existía en storage.ts
    → La card de cada meta muestra el emoji si está seteado, sino muestra Ionicons

  ✓ Donut interactivo con popup de resumen (v1.8)
    → DonutChart envuelto en TouchableOpacity que abre summaryModal
    → Texto "Toca para ver" como centerLabel del donut
    → Popup slide-up: stat boxes Ingresos (verde) + Gastos (rojo)
    → Lista de categorías con color dot, nombre (+ emoji si existe), monto y %
    → Se cierra tocando fuera o con botón "Cerrar"
    → Barra de ahorro eliminada del donut (info duplicada con las pills del hero)

  ✓ Emoji picker en formulario de categorías (v1.7)
    → CategoryFormModal: grid de color reemplazado por grid 5 columnas de 30 emojis
    → CustomCategory ahora tiene campo emoji?: string en storage.ts
    → Color se auto-asigna desde CATEGORY_COLOR_OPTIONS según posición del emoji
    → Preview del modal muestra el emoji grande en lugar de un dot de color
    → resumen.tsx: catGrid muestra emoji si cat.emoji existe, sino usa Ionicons
    → CategoryDetailModal: mismo guard en el header del modal de detalle

  ✓ Mini resumen de metas bajo Patrimonio Neto (v1.7)
    → Card compacta: título "🎯 Metas de ahorro" + contador "X/Y completadas"
    → Barra de progreso global (totalSaved / totalTarget)
    → Fila: Ahorrado (verde) vs Meta total
    → Solo aparece si hay al menos 1 meta registrada

  ✓ FAB con leyenda "Registrar" (v1.7)
    → Envuelto en fabContainer (position: absolute) con alignItems: center
    → Texto pequeño "Registrar" debajo del círculo en color primary
    → Llama la atención para registrar gastos/ingresos

  ✓ Patrimonio Neto más llamativo (v1.7)
    → Borde izquierdo grueso (4px) en color verde o rojo según el signo
    → Fondo con tinte sutil (color + '0D') reactivo al resultado
    → Ícono trending-up/trending-down al lado del título
    → Activos y Pasivos en 2 columnas lado a lado en vez de filas
    → Valor "Neto" en FONT.xl con peso 800

  ✓ Orden visual en tabs DÉBITO y CRÉDITO (v1.6)
    → Tab CRÉDITO: resumen "Total gastado/disponible" se mueve arriba del título "Tarjetas de crédito"
    → Tab DÉBITO: mismo patrón — resumen arriba, nuevo título "Tarjetas de débito" debajo
    → Sección Efectivo: importe del total ahora en verde (COLORS.debit) en vez del ámbar de cash

  ✓ Modal de abono excedido con tema de la app (v1.5)
    → Reemplazado Alert.alert por modal centrado con animación "fade"
    → Ícono de alerta rojo, borde danger, muestra monto ingresado vs pendiente
    → Botón "Entendido, voy a corregirlo" — no cierra el step de abono

  ✓ Título "Tarjetas de crédito" en el tab CRÉDITO (v1.5)
    → sectionDivider agregado arriba de las CardViews de crédito
    → Muestra total gastado a la derecha (en rojo) si hay gastos registrados
    → Consistente con el divider "Efectivo" que ya existía en DÉBITO

  ✓ Saldo inicial en tarjeta preview del panel de préstamos (v1.5)
    → En el preview card del panel de acción, debajo del badge "Préstamo"
    → Muestra el initialBalance en blanco semitransparente (tamaño 11)
    → Si no tiene initialBalance (deudas antiguas), no muestra nada

  ✓ Emoji opcional para todos los tipos de tarjeta (v1.5)
    → Campo "Emoji (opcional)" ahora aparece para debit, credit, cash y debt
    → Placeholder contextual: 🏦 débito · 💳 crédito · 💵 efectivo · 💸 deuda
    → CardView muestra el emoji en la esquina inferior izquierda si está seteado
    → Para crédito: emoji + últimos 4 dígitos en la misma fila
    → AccountRow muestra emoji en el ícono para cualquier tipo de cuenta
    → handleSave guarda emoji para todos los tipos (antes solo para debt)

  ✓ Fecha límite + notificación local para préstamos (v1.5)
    → Nuevo campo dueDate?: string en interfaz Card (storage.ts)
    → En CardFormModal para deuda: campo "Fecha límite de pago" (DD/MM/AAAA)
    → Al ingresar fecha, aparece toggle "Notificarme el día del vencimiento"
    → Toggle activa expo-notifications: pide permiso + schedula notif a las 9AM
    → La fecha se muestra en AccountRow ("Vence: DD/MM/AAAA") y en el panel
    → Panel de acción: ícono de calendario + "Fecha límite" en fila separada

  ✓ Resumen DÉBITO incluye efectivo en "Total disponible" (v1.4)
    → Fila de resumen ahora muestra totalCuentasSpent (debit + cash) en "Total gastado"
    → "Total disponible" = totalBalance (debit + cash combinados)
    → Condición cambiada a cuentas.length > 1 (antes solo debitCards.length > 1)
    → Eliminado el footer "Saldo total disponible" del fondo del tab

  ✓ Validación: abono no puede superar el pendiente por pagar (v1.4)
    → handleAddMoney verifica: si isDebt && amount > balance → Alert.alert
    → Mensaje explica el exceso y pide modificar el valor
    → No registra el evento ni actualiza la tarjeta hasta que sea válido

  ✓ "Deuda inicial" en el panel de acción de préstamos (v1.4)
    → Nuevo campo initialBalance?: number en interfaz Card (storage.ts)
    → CardFormModal.tsx: al crear una deuda, initialBalance = balance (monto ingresado)
    → Al editar, initialBalance se preserva (no cambia)
    → Panel de acción: muestra "Deuda inicial" (en gris/muted) + "Pendiente por pagar" (en rojo)
    → "Pendiente por pagar" disminuye con cada abono; "Deuda inicial" queda fijo

  ✓ Fix CardView compact: lastFour oculto para tarjetas no-crédito (FIXA)
    → CardView.tsx línea 35: lastFour solo se renderiza cuando card.type === 'credit'
    → Evita mostrar número random en selector de tarjetas de Finando IA

  ✓ Limpieza de styles muertos en tarjetas.tsx (FIXA)
    → Eliminados balanceBanner, balanceLabel, balanceAmount del useMemo de styles
    → Eliminado import CardEvent sin usar de tarjetas.tsx

  ✓ Fila "Total gastado / Total disponible" en tab DÉBITO (v1.3)
    → Aparece cuando hay más de 1 tarjeta débito (igual que crédito)
    → Total gastado en rojo · Total disponible en verde
    → Computed: totalDebitSpent, totalDebitAvail

  ✓ Valores de préstamos en rojo COLORS.danger (v1.3)
    → AccountRow: balance de deuda en COLORS.danger (#EF4444)
    → debtSummaryCard: valor total en COLORS.danger
    → Divisor de sección Préstamos: total en COLORS.danger
    → Panel de acción: accentColor para debt cambiado a COLORS.danger

  ✓ "Total pendiente" → "Total deuda" + clickeable (v1.3)
    → debtSummaryCard ahora es TouchableOpacity
    → Chevron (›) a la derecha como indicador visual
    → Al tocar abre modal "Historial de abonos" con TODOS los pagos
      de TODAS las deudas combinados, ordenados por fecha reciente
    → Muestra total abonado acumulado, card por card, con notas

  ✓ "Nombre" → "Alias" en el formulario de tarjeta/deuda/efectivo (v1.2)
    → CardFormModal.tsx: label cambiado a "Alias" en el campo de nombre

  ✓ Tarjetas débito con vista de tarjeta igual que crédito (v1.2)
    → Sección débito usa CardView horizontal (scroll), igual que crédito
    → CardView.tsx: lastFour ocultado para tipo debit; se muestra alias + banco
    → El alias va en grande arriba, el banco en pequeño, sin dígitos

  ✓ Resumen de préstamos en el tab CRÉDITO (v1.2)
    → Card compacta bajo el divisor "Préstamos" mostrando total pendiente + contador
    → Fondo naranja suave con badge "X préstamos" a la derecha

  ✓ Saldo total menos intrusivo y abajo (v1.2)
    → Removido el banner verde grande del top del tab DÉBITO
    → Reemplazado por una fila pequeña al fondo: "Saldo total disponible $X"
    → Solo aparece cuando hay al menos una cuenta registrada

  ✓ Observación opcional en la sección de abonos (v1.2)
    → Campo de texto debajo del monto en el paso de agregar dinero / abonar
    → Se guarda como note?: string en CardEvent (storage.ts)
    → El historial del panel muestra la nota debajo del tipo de evento

  ✓ Botón de tema claro/oscuro en pantalla de bienvenida (app/onboarding.tsx)
    → Ícono flotante arriba a la derecha (sol/luna) sobre el step 'welcome'
    → Usa useThemeInfo() para alternar entre 'light' y 'dark', persistido en AsyncStorage

  ✓ Pantalla de elección tras "Únete a la comunidad 🚀" (app/onboarding.tsx)
    → Nuevo step 'choice' con 3 opciones: Crear cuenta nueva / Iniciar sesión / Modo anónimo
    → La opción de modo anónimo aclara que la IA de Finando estará desactivada

  ✓ Flujo de "Iniciar sesión" (app/onboarding.tsx)
    → Nuevo step 'login': compara email + contraseña contra el perfil guardado localmente
    → Si coincide entra a la app; si no, muestra alerta de credenciales no encontradas

  ✓ Modo anónimo + bloqueo de Finando IA
    → "Modo anónimo" crea un perfil con isAnonymous: true (lib/storage.ts) y entra directo a la app
    → app/(tabs)/index.tsx detecta isAnonymous y muestra pantalla de IA bloqueada en vez del chat
    → app/(tabs)/perfil.tsx muestra "Modo anónimo · Finando IA desactivada" en lugar del correo

  ✓ Selector de emoji para avatar en creación de cuenta (app/onboarding.tsx)
    → Campo de texto junto a la paleta de colores que acepta un emoji del teclado del dispositivo
    → avatarEmoji se guarda en UserProfile y se muestra en vez de las iniciales (onboarding y perfil)

  ✓ Leyenda de privacidad más profesional (app/onboarding.tsx)
    → Reemplazado "Tus datos nunca salen de este dispositivo..." por
      "🔒 Seguridad garantizada: tus datos se almacenan de forma local y privada en tu dispositivo."

  ✓ Pantalla de Perfil/Configuración (app/(tabs)/perfil.tsx)
    → Tab "Perfil" en la barra de navegación
    → Secciones: avatar + nombre editable, tema, idioma, versión, cerrar sesión
    → Botón de tema con 3 opciones: Sistema / Claro / Oscuro (persiste en AsyncStorage)
    → Cerrar sesión borra el perfil y redirige a onboarding

  ✓ Renombrar "Cuentas" → "Débito" en la pantalla Balance
    → Tab renombrado de "🏦 Cuentas" a "🏦 Débito"
    → Botones de agregar cambiados a "Agregar tarjeta débito"
    → CardFormModal bloqueado a tipo debit cuando se abre desde este tab

  ✓ Tab "💵 Efectivo" en Balance
    → Tercer sub-tab para registrar dinero en efectivo
    → Mismo funcionamiento que tarjetas débito (saldo + gastos)
    → Icono diferenciado (cash-outline), no requiere banco ni lastFour
    → type 'cash' agregado a la interfaz Card en storage.ts

  ✓ COLORS is not defined en tarjetas.tsx
    → Todos los screens migrados a useColors() + useMemo

  ✓ ThemeProvider undefined en _layout.tsx (Render Error)
    → Movido import de expo-router a @react-navigation/native

  ✓ Pantalla en blanco al cargar la app
    → SplashScreen.hideAsync() movido a efecto separado
       que espera loaded && checked antes de ejecutarse

  ✓ router.replace('/onboarding') antes de que Stack esté montado
    → Envuelto en setTimeout(0) para diferir al siguiente ciclo

  ✓ Metro puerto 8081 ocupado (error recurrente)
    → Usar taskkill /PID $portPID /F antes de iniciar Metro

  ✓ Sub-tabs con leyendas diferenciadas y contador por tipo (v0.9)
    → "🏦 Cuentas" activo en verde (COLORS.debit), "💳 Crédito" activo en rojo (COLORS.credit)
    → Badge con conteo de cuentas aparece en la pill activa
    → La unión Débito+Efectivo en la misma pestaña Cuentas se mantiene fusionada

  ✓ Eliminado campo de últimos 4 dígitos en cuentas débito (v0.9)
    → AccountRow muestra solo el nombre del banco para débito, sin "••XXXX"
    → Para efectivo muestra "Efectivo", para deudas muestra "Deuda pendiente"
    → El campo lastFour solo sigue siendo visible en tarjetas de crédito (CardView)

  ✓ Modal de acción hermoso al tocar una cuenta (v0.9)
    → Reemplazado Alert.alert de long-press por bottom sheet personalizado con tema de la app
    → Muestra mini vista previa de la tarjeta con su color y nombre
    → Opciones: Editar / Eliminar / Agregar dinero (débito/efectivo) o Abonar deuda (deuda)
    → "Agregar dinero" suma al balance; "Abonar" resta del monto de deuda
    → Paso 2: input numérico centrado con botón de confirmación en color del tipo de cuenta

  ✓ Sección de Préstamos en pestaña Crédito (v0.9)
    → Nuevo tipo 'debt' con su color naranja (#F97316) y fondo en constants/theme.ts
    → Sección "Deudas pendientes" visible siempre en el tab Crédito, con total en naranja
    → Crear deuda desde CardFormModal con campos: nombre + monto (sin banco, sin lastFour)
    → Se puede abonar a cada préstamo desde el modal de acción (reduce el monto pendiente)

  ✓ Panel de acción con un solo toque (v1.0)
    → Cambiado de onLongPress a onPress en todas las filas (débito, efectivo, crédito, préstamos)
    → Eliminada la selección con expansión de detalle debajo — el detalle pasa al panel

  ✓ Mini historial en el panel de acción (v1.0)
    → Nuevos CardEvent (deposit/pay) guardados en Card.events via appendCardEvent en storage.ts
    → Panel muestra últimos 3 eventos de depósito/abono + últimos 3 gastos por tarjeta
    → Para crédito muestra cupo gastado / disponible / límite + barra de progreso

  ✓ Pills "🏦 Débito" / "💳 Crédito" (v1.0)
    → Etiqueta "Cuentas" cambiada a "Débito" en la pill activa verde

  ✓ Renombrado "Deudas pendientes" → "Préstamos" (v1.0)
    → Cambiado en todos los textos: pills, botones, subtítulos, TYPE_META, panel

  ✓ Emoji opcional para préstamos (v1.0)
    → Nuevo campo emoji? en interfaz Card (storage.ts)
    → CardFormModal muestra input de emoji para tipo debt (opcional, maxLength 2)
    → AccountRow y panel de acción muestran el emoji en lugar del ícono si está seteado

  ✓ Formateo automático de miles (v1.0)
    → Helper fmt() con toLocaleString('es-CO') en CardFormModal y tarjetas.tsx
    → Aplica en: saldo, límite, monto del préstamo, y campo "Agregar dinero" del panel

  ✓ Patrimonio Neto en Resumen (v1.0 — adición PECAS)
    → Tarjeta entre el hero y las cards de débito/crédito
    → Activos = debit disponible + cash disponible
    → Pasivos = crédito gastado + total préstamos
    → Neto = verde si positivo, rojo si negativo
    → Solo aparece si hay al menos un activo o pasivo registrado

------------------------------------------------------------
[ERRORES REGISTRADOS POR FIXA]
------------------------------------------------------------

  2026-06-15 | Metro caché corrupto
    Síntoma: "COLORS is not defined" aparecía aunque el código era correcto
    Causa: bundle antiguo en caché de Metro
    Fix: npx expo start --clear

  2026-06-15 | ThemeProvider undefined
    Síntoma: Render Error "Element type is invalid: got undefined" en RootLayout
    Causa: expo-router v6 no exporta ThemeProvider
    Fix: import { ThemeProvider } from '@react-navigation/native'

  2026-06-15 | Blank screen sin error
    Síntoma: App cargaba pero pantalla quedaba completamente en blanco
    Causa: SplashScreen.hideAsync() llamado mientras layout retornaba null
    Fix: useEffect separado que solo ejecuta cuando loaded && checked = true

========================================
