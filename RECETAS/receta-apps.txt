====================================================
 RECETA DE DESARROLLO DE APPS - BITACORA PASO A PASO
====================================================
Este archivo documenta, como una receta de cocina, todo lo que se
va haciendo (y lo que falta por hacer) en la creacion de apps dentro
de este workspace usando la skill PECAS.

Se actualiza automaticamente cada vez que se ejecuta /compact
(manual o automatico), agregando una nueva entrada al final del
Historial de Cambios con fecha y hora.

----------------------------------------------------
IDEA / PROYECTO ACTUAL
----------------------------------------------------
Estado: EN DEFINICION
Aun no se ha descrito la idea de la app. Pendiente de responder:
  1. Que hace exactamente la app (una oracion)
  2. Web, movil o escritorio
  3. Necesita login de usuarios (Supabase Auth)
  4. Necesita cobrar (Stripe: pago unico o suscripcion)
  5. Cual es el MVP minimo vendible

----------------------------------------------------
PASOS DE LA RECETA (se van llenando conforme avanza el proyecto)
----------------------------------------------------
[ ] Paso 0 - Definir la idea con las 4 preguntas de PECAS
[ ] Paso 1 - Elegir stack (web/movil/escritorio) y hacer setup inicial
[ ] Paso 2 - Estructura base del proyecto (carpetas, .env, .gitignore)
[ ] Paso 3 - Auth (si aplica)
[ ] Paso 4 - Feature principal end-to-end
[ ] Paso 5 - Pagos (si aplica)
[ ] Paso 6 - Checklist de MVP listo
[ ] Paso 7 - Deploy / build de produccion
[ ] Paso 8 - Estrategia de venta (Gumroad / Lemon Squeezy / Product Hunt)

----------------------------------------------------
LECCIONES GENERALES — aplican a CUALQUIER app nueva
----------------------------------------------------
Extraidas de la auditoria completa de wallet-control (2026-07-02).
Aplicar desde el Paso 0/1 de la receta, no al final del proyecto.

[SEGURIDAD]
- Nunca llamar APIs de terceros con API key (Anthropic, OpenAI, etc.)
  directo desde el cliente usando variables EXPO_PUBLIC_* / NEXT_PUBLIC_*.
  Esas variables quedan incrustadas en el bundle de produccion y son
  extraibles descompilando el build. Si la app necesita IA real, la
  llamada debe pasar por un backend/proxy propio (Cloudflare Worker,
  Vercel/Supabase Edge Function) que guarde la key del lado servidor.
  Mientras no exista ese proxy, usar un "modo demo" con respuestas
  simuladas como modo publico por defecto.
- No fingir seguridad: si una app guarda "password" pero no hay backend
  real detras (todo el storage es local), ese campo no protege nada.
  O se quita, o se usa expo-secure-store / Keychain / Keystore. Nunca
  decirle al usuario en la UI que algo "es seguro" si no lo es.

[SETUP TEMPRANO — evita bloquear el primer build]
- Configurar bundleIdentifier (iOS) / package (Android) en app.json
  desde el Paso 1 de setup, no cuando ya se quiere compartir la app.
  Sin esto, EAS Build no puede generar credenciales.
- Correr "eas build:configure" apenas se decide que la app movil se va
  a compartir en algun momento — deja eas.json listo con anticipacion.
- Cada vez que se instala una libreria que requiere plugin nativo
  (expo-notifications, expo-camera, etc.), declararla en app.json
  "plugins" en el mismo momento de instalarla, no despues.

[NOTIFICACIONES]
- Al usar expo-notifications, configurar Notifications.setNotificationHandler
  desde el Paso 1 de setup (en el _layout raiz, a nivel de modulo, no dentro
  de un componente). Sin este handler, las notificaciones locales
  (scheduleNotificationAsync) no se muestran mientras la app esta en primer
  plano — el sintoma es "activo la notificacion pero no pasa nada" al probar
  dentro de la app misma. Detectado en wallet-control: la notificacion de
  saldo (toggle en Perfil) no se veia porque nunca se habia registrado este
  handler, pese a que el resto del flujo (permiso, scheduleNotificationAsync)
  estaba bien implementado.

[ASSETS]
- Exportar icon.png / splash.png ya limpios (sin guias de diseno:
  circulos, lineas punteadas, marcas de referencia) antes de marcar el
  diseno visual como terminado. Revisar visualmente el asset final, no
  solo confirmar que el archivo existe.

[EVITAR FEATURES A MEDIAS]
- No dejar componentes construidos pero nunca montados en ninguna
  pantalla (ej. una barra de progreso de presupuesto que existe en
  codigo pero no aparece en ningun lado) ni promesas de "proximamente"
  en la UI sin que esa feature este realmente en el backlog.
- Antes de compartir cualquier build fuera del circulo de desarrollo,
  correr un checklist rapido de auditoria (seguridad + capacidad real
  de generar build + consistencia UI) en vez de asumir que "se ve
  terminado" porque las pantallas principales funcionan.

[ESTRUCTURA DE NOTAS]
- Usar desde el inicio del proyecto la misma carpeta NOTAS/ estandar:
  ACTUALIZACIONES.txt (changelog), "ajustes [app].txt" (bugs/tareas,
  MEJORAX), "ideas [app].txt" (backlog priorizado), metricas.txt
  (sistema visual fijo, NO MODIFICAR), palabras-clave.txt (comandos).
  Evita tener que reorganizar archivos sueltos despues.

[RESPONSIVE DESIGN]
- Instalar y configurar useWindowDimensions (hook nativo de React
  Native) desde el Paso 1 de setup en toda app movil nueva, no como
  retrofit despues de que las pantallas ya tienen estilos fijos.
  Preferir un hook propio sobre useWindowDimensions (reactivo a
  rotacion/resize) en vez de librerias como react-native-size-matters,
  que calculan Dimensions.get('window') una sola vez al importar el
  modulo y no se actualizan. Definir desde el inicio un helper de
  escalado (scale/verticalScale/moderateScale) y envolver los
  StyleSheet.create de cada pantalla con el, en vez de escribir
  fontSize/padding/width fijos en numeros sueltos. Detectado como
  deuda tecnica en wallet-control: hubo que agregar esto de golpe a
  ~10 pantallas ya maduras en vez de tenerlo desde el principio.

[CAMINO DE LANZAMIENTO RECOMENDADO]
1. Build APK con "eas build --profile preview" -> compartir con 5-10
   personas de confianza (reemplaza depender de Expo Go + servidor).
2. Beta cerrada 2-3 semanas: feedback especifico antes de audiencia
   mas grande. En paralelo, subir a Google Play Internal Testing.
3. Landing simple (skill PAKI) en paralelo, con 2-3 screenshots reales.
4. Lanzamiento publico: Play Store / App Store + Product Hunt +
   comunidades relevantes al nicho de la app.
5. Ciclo continuo: usar el propio ACTUALIZACIONES.txt como base para
   publicar novedades y mantener audiencia activa.

----------------------------------------------------
HISTORIAL DE CAMBIOS
----------------------------------------------------

[2026-07-01] Sesion inicial
- Se activo la skill PECAS para desarrollar una nueva idea de app.
- El usuario aun no describio la idea; quedaron pendientes las 4
  preguntas de la Fase 0 de PECAS (que hace, plataforma, auth, pagos, MVP).
- El usuario pidio documentar todo el proceso de creacion de la app en
  este archivo, en formato receta paso a paso, guardado en:
  C:\Users\Lenovo Ideapad\Documents\GitHub\claude-workspace\RECETAS
- Se configuro un hook PostCompact (en .claude/settings.local.json) que,
  cada vez que se ejecuta /compact, recuerda automaticamente actualizar
  este archivo con lo nuevo hecho en la sesion.

[2026-07-01] Fix de hook + comando fijo para abrir servidor wallet-control
- El hook PostCompact fallo la primera vez que se ejecuto /compact: el
  schema de Claude Code no permite "hookSpecificOutput.additionalContext"
  para el evento PostCompact (solo aplica a PreToolUse, UserPromptSubmit,
  PostToolUse, PostToolBatch, Stop/SubagentStop).
- Se corrigio el hook para que el comando emita texto plano en vez de JSON
  con hookSpecificOutput. Se valido que settings.local.json sigue siendo
  JSON valido.
- El usuario establecio una instruccion fija: cada vez que pida "abrir el
  servidor" (de wallet-control), ejecutar el script de PowerShell que abre
  Expo en el puerto 8082 (Start-Process powershell -NoExit -Command
  "cd wallet-control; npx expo start --port 8082"). Se ejecuto una vez
  para abrir el servidor ahora mismo.

[2026-07-02] Lecciones generales desde auditoria de wallet-control
- Se hizo una auditoria completa de wallet-control (codigo real, no solo
  notas): seguridad (API key expuesta en bundle, password en texto
  plano), bloqueantes de build (bundleIdentifier/package/eas.json
  faltantes, plugin de notificaciones sin declarar, assets con guias de
  diseno sin limpiar) y features a medias (componentes sin montar,
  promesas de UI sin cumplir).
- Se agrego la seccion "LECCIONES GENERALES" a este archivo con los
  puntos que aplican a cualquier app nueva creada con PECAS, para
  aplicarlos desde el Paso 0/1 en vez de descubrirlos al final.

[2026-07-04] Leccion de responsive design + aclaracion de archivo de recetas
- Durante el trabajo de fase 1 de wallet-control se detecto que ninguna
  app del workspace (ni wallet-control ni ruta-segura) tiene un sistema
  real de responsive design (useWindowDimensions, scale/moderateScale,
  etc.) — todos los tamanos estan fijos en StyleSheet.create.
- Se agrego la seccion "RESPONSIVE DESIGN" a las lecciones generales de
  este archivo para que las apps nuevas lo incluyan desde el Paso 1.
- El usuario aclaro que, de ahora en adelante, cuando se refiera al
  "archivo de recetas" es este archivo (RECETAS/receta-apps.txt), no
  otro documento.

[2026-07-06] Sesion larga: cierre Fase 1, Fase 2, Fase 3 y 2 rondas de MEJORAX en wallet-control
- Fase 1 (bloqueantes de build): se genero un icono/splash real por codigo
  (reemplazando el default literal de la plantilla de Expo) y se creo
  eas.json con los perfiles de build. Quedo pendiente el "eas login" /
  "eas init" (requiere cuenta propia del usuario) — anotado en
  ajustes wallet-control.txt bajo [PENDIENTE] por MAQUINA.
- Fase 2 (pulido): exportacion a PDF tipo "extracto bancario" reemplazando
  la promesa incumplida de Excel/CSV; parser de markdown propio (sin
  libreria nueva) para el chat de Finando; preferencia de periodicidad de
  cuenta (mensual/quincenal/semanal) elegida en el onboarding, conectada a
  QuincenaCard/SemanaCard/MesCard; version de la app sincronizada con
  expo-constants; notificaciones de presupuesto (80%/100%) y gastos
  recurrentes con expo-notifications; primera idea "MIA" del backlog:
  2 preguntas de onboarding para ingreso/gastos fijos que crean
  Income/Expense recurrentes reales (no solo referencia).
- Fase 3 (roadmap): investigado un widget nativo real de saldo en pantalla
  de inicio — no viable sin salir de Expo Go (requeriria
  react-native-android-widget + expo prebuild + build nativo real);
  reemplazado por notificacion local opt-in de saldo (toggle en Perfil).
  Se agrego busqueda de gastos (pantalla nueva app/busqueda.tsx, filtro por
  nombre/categoria/fecha) y comparativa de gasto mes a mes en Resumen.
- Bugs reales resueltos en vivo: cache de Metro con bundle viejo (fix:
  matar el proceso del puerto 8082 + "npx expo start --clear"); error de
  Supabase "columna email no encontrada" (migracion SQL existente en el
  repo nunca se habia corrido contra el proyecto real); error de RLS al
  registrar cuenta con correo real, causado por "Confirm email" de
  Supabase + insercion en profiles antes de tener sesion confirmada —
  mitigado temporalmente desactivando "Confirm email", con el fix de
  codigo correcto anotado como pendiente para antes del lanzamiento
  publico; overflow de texto en montos grandes en pantallas/fuentes
  distintas — fix con adjustsFontSizeToFit + numberOfLines={1} en todos
  los Text de montos grandes de la app.
- MEJORAX rondas 1 y 2 (pendientes que el usuario fue anotando el mismo en
  ajustes wallet-control.txt): formato de miles automatico en todos los
  inputs numericos; rediseno del step de gastos fijos del onboarding a
  lista con chips + multiples items; ojo mostrar/ocultar contraseña +
  medidor de fuerza en el onboarding; Finando personalizado por cuenta
  (ya no usa una lista de categorias ni ejemplo con montos fijos
  compartido para todos los usuarios, ahora usa las categorias reales de
  cada cuenta); renombrado "extracto bancario" a "extracto de tu cuenta"
  en toda la UI y el PDF; aislamiento de datos local por cuenta en
  lib/storage.ts (namespacing de claves de AsyncStorage por profile.id,
  con migracion no-destructiva de los datos previos al namespace activo).
- Se establecio una convencion nueva con el usuario: de ahora en adelante
  "actualiza el plan" (para wallet-control) significa regenerar
  PDF/wallet-control-avance.pdf (via Puppeteer, con el sistema visual ya
  fijo de la app), no el plan interno de Claude Code.
- Auditoria final de Fase 3: quedan 2 hallazgos reales sin resolver (aviso
  de permiso de notificaciones denegado en CardFormModal.tsx, y falta de
  accessibilityLabel/Role en toda la app) — documentados en el PDF junto
  con el plan final de publicacion a Play Store.

[2026-07-07] Fix de notificacion de saldo + leccion de setNotificationHandler
- Se probo en dispositivo la Fase 3 de wallet-control: la notificacion de
  saldo (toggle en Perfil) no se disparaba al entrar a Resumen. Causa: nunca
  se habia configurado Notifications.setNotificationHandler, por lo que
  expo-notifications no muestra notificaciones locales con la app en
  primer plano. Se agrego el handler en app/_layout.tsx.
- Se agrego la seccion "NOTIFICACIONES" a las lecciones generales de este
  archivo para configurar el handler desde el Paso 1 en cualquier app nueva
  que use expo-notifications.
- Otros 2 ajustes de la misma ronda de pruebas (especificos de wallet-control,
  no lecciones generales): filtro de categorias en busqueda de gastos ahora
  solo muestra categorias con gastos reales; la card de "mes anterior" en
  Resumen ahora abre el mismo detalle de activos/pasivos que el mes actual
  en vez de una grafica de tendencia.

[2026-07-08] Auditoria completa de ruta-segura + fixes FIXA/MEJORAX + logo real
- AUDITA genero 2 PDFs en APPS/ruta-segura/AUDITORIAS/ y PDF/: auditoria tecnica
  completa (uso interno) y brief corto para el freelancer, separando lo que
  funciona de verdad (SOS real via Linking.openURL, WhatsApp real, bilingue
  ES/EN, navegacion) de lo que es fachada visual sin backend (registro,
  contenido hardcodeado, contador "mujeres en linea" con numero aleatorio,
  MiCaso sin persistencia).
- FIXA convirtio la auditoria en lista de prioridad (menor a mayor esfuerzo) y
  se ejecutaron los 5 "quick win" + tier "chico": limpieza de app.json
  (minSdkVersion invalido eliminado, version bajada de 1.0.0 a 0.9.0 real),
  borrado de codigo muerto (InicioScreen.js, 2 assets sin uso, imagen suelta en
  raiz), validacion real de email en RegistroScreen.js (regex + boton
  deshabilitado + error inline en vez de solo placeholder), eliminado bloque
  decorativo de "Adjuntos" en MiCasoScreen.js (prometia Foto/Audio/Archivo sin
  funcionar), ajustada lista de estados en EstadoScreen.js (fusion de Carolina
  del Norte/Sur en "Carolinas", quitados Arizona/Colorado poco representados),
  corregido "Asesora" -> "Consultora de inmigracion" en CategoriaScreen.js.
- Se elimino el contador falso "mujeres en linea" (numero random en
  MenuPrincipalScreen.js) por texto estatico honesto ("Estas en un espacio
  seguro - No estas sola"), pero se documento como idea futura en
  NOTAS/ideas app ruta.txt con la regla explicita: si se implementa de verdad
  algun dia, debe ser con datos reales, nunca simulados otra vez.
- MEJORAX corrio sobre el backlog de 8 items que la clienta (Ana Laverde) fue
  dictando en vivo: resueltos los accionables de codigo ese mismo momento;
  documentados en NOTAS los que son solo de posicionamiento/copy (tagline
  "MIGRAR CON PROPOSITO"); dejados en bloqueado/en progreso los que requerian
  un archivo del usuario (logo real) o acceso a metricas.txt (protegido por
  hook, no editable por Claude Code).
- Reemplazado el placeholder de marca de agua "AL" (circulo con iniciales) por
  el logo real de Ana's Immigration Solutions (assets/ana-laverde-logo.png)
  en las 7 pantallas que lo usan (Onboarding, Registro, Estado, MenuPrincipal,
  Categoria, MiCaso, SOS), manteniendo siempre el nombre "Ana Laverde" visible
  al lado — nunca se modifico la marca de la clienta, solo el icono.
- Pendiente real que sigue bloqueado: escritura en NOTAS/metricas.txt (hook
  block-notas-readonly.ps1 impide a Claude Code tocarlo) — se le paso al
  usuario el texto exacto + pasos para pegarlo el mismo manualmente, cubriendo
  atribucion de autoria en package.json/App.js y una marca sutil "N" en la UI
  sin interferir con la marca visible de Ana Laverde.
- Divergencia de GitDoc detectada (local 7 commits adelante, remoto 1 commit
  del hermano) — diagnosticada solo en modo lectura (git fetch/log/show
  --stat), advertido explicitamente NO usar "Force Push" (borraria el commit
  del hermano); resolucion dejada pendiente para el usuario, sin merge/rebase
  automatico de por medio (regla fija de CLAUDE.md).

[2026-07-12 / 2026-07-13] Sesion larga: VISOR+scrcpy, rediseno del logo, IMAGINA, y plan "corrige todo" (unificacion + persistencia + subida Expo/RN)
- Se le puso nombre a la capacidad de ver/controlar el celular real instalada en
  una sesion anterior: "VISOR" (mobile-mcp + adb), confirmado que aplica a todo
  el repo (no solo a un proyecto). Se instalo ademas scrcpy (via winget) para que
  el usuario pueda ver el espejo de la pantalla del celular en su propio PC (esto
  si es solo para el humano, Claude Code no lo usa directamente).
- Se aclararon los casos de uso de cada herramienta de visualizacion (VISOR vs
  ESTETIK vs IMAGINA vs scrcpy) en una comparativa a pedido del usuario.
- Se hizo portable el comando de "abrir el servidor": reemplazadas rutas
  absolutas quemadas (Z:\... / rutas de una sola maquina) por
  -WorkingDirectory relativo en .claude/settings.local.json, tanto para
  wallet-control como para ruta-segura, para que funcione igual en la maquina
  del usuario y en la de su hermano.
- Se diagnostico y arreglo en vivo (dos veces) un servidor Expo caido
  ("Failed to download remote update" en el celular): el proceso de
  "npx expo start --port 8082" se habia muerto en segundo plano; fix
  reiniciarlo + "adb reverse tcp:8082 tcp:8082" + recargar la app.
- ESTETIK audito el logo de marca de agua de Ana Laverde (circulo de corona de
  laurel incompleto, problema de gramatica "Immigrations", redundante con el
  texto "Ana Laverde" ya visible). Tras varias rondas de iteracion con el
  usuario -incluyendo 2 correcciones explicitas de NO redibujar ni modificar
  el simbolo original (ni las hojas doradas ni la balanza)- se opto por usar
  "sharp" (Node.js) para recortar el archivo real (nunca regenerar arte nuevo):
  resultado final es un recorte circular real (mascara alpha via SVG
  dest-in) del logo original, sin el nombre del negocio ni el tagline, solo
  corona+hojas+balanza+"AIS", centrado y balanceado a pedido del usuario.
  Nuevo asset: assets/ana-laverde-logo-circle.png (reemplaza al logo
  cuadrado/rectangular en las 7 pantallas que usan la marca de agua; el logo
  original completo se conserva sin borrar por si se necesita para PDFs/
  marketing a futuro).
- "IMAGINA" genero un mini-PDF de UX (PDF/ruta-segura-imagina-2026-07-12.pdf)
  con 5 hallazgos; PECAS implemento los primeros 4 (altura uniforme de
  tarjetas en MenuPrincipal/Estado con numberOfLines+minHeight, contraste de
  marca de agua en SOSScreen subido de opacity 0.5 a 0.85, reestructuracion de
  RegistroScreen para centrar el bloque de seleccion dejando "Volver" fijo
  arriba) dejando fuera, a peticion explicita del usuario, el ultimo
  (consistencia de forma de los botones flotantes).
- AUDITA actualizo el informe de seguimiento (AUDITORIAS/auditoria-2026-07-12.pdf)
  comparando contra el del 8 de julio: ~40-45% -> ~55% de avance real, 10 items
  resueltos, bugs pendientes/nuevos (incluyendo uno recien descubierto: el fix
  de icono de "alimentacion" habia quedado incompleto -ver mas abajo), y numeros
  frescos de npm audit/outdated/expo-doctor.
- El propio proceso de auditoria destapo el bug raiz: CategoriaScreen.js tenia
  su propia copia separada de los datos de categoria (CONTENIDO), nunca
  sincronizada con el CATEGORIAS de MenuPrincipalScreen.js -por eso el icono de
  "alimentacion" seguia mostrando el mazo de juez en el detalle pese a estar ya
  arreglado en el menu, y "comunidad" tenia el problema inverso.
- El usuario pidio "corrige todo de una vez en un plan": en Plan Mode se
  investigo el codigo real y se corrieron npm audit/outdated en vivo antes de
  proponer alcance; el usuario eligio el nivel mas amplio ofrecido: fixes de
  codigo + persistencia de sesion + npm audit + subir Expo 54->57 / React
  Native 0.78->0.86 (aceptando el riesgo), dejando fuera de forma explicita
  Supabase real, EAS init, contenido de politica de privacidad, GPS/mapas,
  notificaciones push y cualquier UI de cerrar sesion.
- Plan Paso 1 (unificacion de datos) completado: nuevo data/categorias.js como
  fuente unica de verdad (CATEGORIAS + getCategoria()), MenuPrincipalScreen.js
  y CategoriaScreen.js ahora importan de ahi en vez de tener copias locales;
  eliminada la rama muerta de renderizado condicional por "imagen" (ya ninguna
  categoria la usa); borrado assets/gavel-icon.png tras confirmar que ya no
  tenia ninguna referencia en el repo.
- Plan Paso 2 (persistencia de sesion) completado: instalado
  @react-native-async-storage/async-storage (mismo patron fail-open que
  wallet-control/lib/storage.ts); nuevo lib/storage.js (getSession/saveSession,
  sin clearSession porque no hay funcion de logout en alcance); agregado el
  flag modoAnonimo al flujo Registro->Estado->MenuPrincipal; App.js reescrito
  con un loading-gate (espera getSession() antes de montar el Stack.Navigator,
  ya que initialRouteName de React Navigation solo se lee una vez al montar).
  Verificado en dispositivo real (VISOR): tras cerrar la app por completo y
  reabrirla, entra directo a MenuPrincipal con los mismos datos guardados.
- Plan Paso 3 (subida de Expo/RN) en progreso: quitado "newArchEnabled": false
  de app.json (React Native >=0.84 ya no tiene arquitectura legacy); corrido
  "npx expo install expo@^57.0.0" y luego "npx expo install --fix" (actualizo
  expo-linear-gradient, expo-status-bar, react, react-native,
  react-native-safe-area-context, react-native-screens al set compatible con
  SDK 57; agrego automaticamente el plugin "expo-status-bar" a app.json).
  Pendiente: subir a mano @react-navigation/native y
  @react-navigation/native-stack (no son paquetes gestionados por Expo, asi
  que "expo install --fix" no los toca), correr expo-doctor y npm audit para
  confirmar, reiniciar el servidor y repetir la verificacion completa de las 7
  pantallas en dispositivo real.
- Tarea paralela sin resolver: configurar 2 dispositivos ADB simultaneos
  (una tablet Huawei como principal por USB + el celular ocasional por WiFi).
  El emparejamiento inalambrico (adb pair/connect) fallo repetidamente porque
  el puerto de depuracion inalambrica de Android rota seguido; se opto por el
  metodo mas estable "adb tcpip 5555" (requiere conectar el celular por USB
  una vez), pero al reconectar el cable "adb devices -l" quedo completamente
  vacio (ni siquiera la tablet que antes si aparecia), incluso despues de un
  "adb kill-server"/"start-server" completo -diagnostico en curso, se le pidio
  al usuario confirmar fisicamente el cable/puerto usado.

----------------------------------------------------
(las entradas nuevas se agregan debajo de esta linea)
----------------------------------------------------
